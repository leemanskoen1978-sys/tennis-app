---
phase: 05.1-groepssleutel
plan: 04
type: execute
wave: 4
depends_on: ["05.1-03"]
files_modified:
  - app/admin/trainingen-import.tsx
  - lib/i18n-en.ts
autonomous: false
requirements: [IMP-12, IMP-13]
must_haves:
  truths:
    - "De droogloop toont vóór het importeren per groep hoeveel komende lessen een andere trainer krijgen, en welke trainer dat is."
    - "Na het importeren zegt het resultaat hoeveel lessen er van trainer gewisseld zijn."
    - "Elke nieuwe Nederlandse zin heeft een Engelse tegenhanger in `lib/i18n-en.ts`."
    - "De beheerder heeft met eigen ogen gezien dat een bestand met een andere Coach de wissel meldt en geen tweede groep aanmaakt."
  artifacts:
    - path: "app/admin/trainingen-import.tsx"
      provides: "De trainerwissel in de droogloop en in het resultaat"
      contains: "trainerwissels"
    - path: "lib/i18n-en.ts"
      provides: "De Engelse kant van de nieuwe zinnen"
      contains: "lessons"
  key_links:
    - from: "app/admin/trainingen-import.tsx"
      to: "ImportPlanLessen.trainerwissels"
      via: "een eigen kaart in de droogloop"
      pattern: "plan.trainerwissels"
    - from: "app/admin/trainingen-import.tsx"
      to: "ImportUitslagLessen.bijgewerkteLessen"
      via: "de resultaatzin"
      pattern: "bijgewerkteLessen"
---

<objective>
Het importscherm toont de trainerwissel vóórdat de beheerder op Importeren drukt: welke groep,
welke trainer, en om hoeveel komende lessen het gaat. Achteraf zegt het resultaat hoeveel lessen er
gewisseld zijn.

Purpose: D-10 — de app raadt niet of een `coach_id` met de hand gezet is. Ze laat de wijziging
zichtbaar zijn met het aantal erbij, en de beheerder bevestigt. Beter zichtbaar dan slim. Zonder
deze kaart is de belofte van IMP-13 wel berekend maar niet nagekomen.
Output: één sectie in de droogloop, één zin in het resultaat, hun Engelse tegenhangers, en één
handmatige controle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-groepssleutel/05.1-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Wat de uitvoerder nodig heeft; geen zoektocht door de codebase. -->

Na plan 05.1-03 bestaat in `lib/import-trainingen.ts`:
```ts
export interface TrainerWissel {
  groep: string; van: string; naar: string; trainerId: string;
  aantal: number; boekingIds: string[];
}
export interface ImportPlanLessen { /* ... */ trainerwissels: TrainerWissel[]; }
export interface ImportUitslagLessen {
  spelers: number; nieuweGroepen: number; bijgewerkteGroepen: number; lessen: number;
  bijgewerkteLessen: number; fouten: ImportFoutLessen[];
}
```

In `app/admin/trainingen-import.tsx` staat vandaag:
- regel 91-92: `function uurTekst(uur: number, minuut: number): string`
- regel 209-215: `groepsRegel(g)` → `` `${g.naam} · ${dag} ${uurTekst(...)} · ${g.trainerNaam} · ${spelers}` ``, met `DAY_LABELS` uit `lib/slots` door `t()`
- regel 333-336: `const nietsNieuws = plan.groepenNieuw.length === 0 && plan.groepenBijgewerkt.length === 0 && ...` — bepaalt of de knop Importeren uitstaat
- regel 344-380: de kop `Dit gaat er gebeuren` / `Resultaat`, de tellingen, en `{n} lessen staan al goed en blijven zoals ze zijn.`
- regel 414-416: `groepenKaart(t('Nieuwe lesgroepen'), plan.groepenNieuw)` en twee zusjes
- regel 434-455: de kaarten `Met de hand verzet of afgezegd; dit blijft zoals het staat` en `Deze lessen staan in de app maar niet meer in het bestand` — dit is de vorm waarin een lijst meldingen getoond wordt; volg die.
- regel 522-530: het bevestigingsblok `Zeker weten?` met de tekst over wat er hierna echt in de app staat.

`lib/i18n-en.ts` is één `Record<string, string>` met de Nederlandse zin letterlijk als sleutel,
inclusief plaatshouders van de vorm `{naam}`. Staat een zin er niet in, dan blijft het Nederlands
staan — een zichtbaar gat en geen stille fout.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Taak 1: De trainerwissel in de droogloop en in het resultaat</name>
  <files>app/admin/trainingen-import.tsx</files>

  <read_first>
    - `app/admin/trainingen-import.tsx` — regels 85-230: `uurTekst`, de schermstaat, `groepsRegel`
    - `app/admin/trainingen-import.tsx` — regels 330-470: de hele droogloopweergave, in het bijzonder de kaarten rond regel 434-455 als vorm om na te volgen
    - `app/admin/trainingen-import.tsx` — regels 470-560: het resultaat, het bevestigingsblok en de knoppen
    - `lib/import-trainingen.ts` — `TrainerWissel`, `ImportPlanLessen.trainerwissels`, `ImportUitslagLessen.bijgewerkteLessen`
    - `.planning/phases/05.1-groepssleutel/05.1-CONTEXT.md` — D-07 en D-10, over wat er getoond moet worden en waarom
  </read_first>

  <action>
    Voeg tussen de groepenkaarten en de kaart over met de hand verzette lessen een eigen sectie toe,
    in dezelfde vorm als de bestaande waarschuwingskaarten. Kop:
    `t('Deze lesgroepen krijgen een andere trainer')`. Eronder per wissel één regel:
    `t('{groep}: {aantal} komende lessen gaan van {van} naar {naar}.', { groep, aantal, van, naar })`.
    Is `van` leeg — de oude trainer heeft geen account meer — gebruik dan de variant
    `t('{groep}: {aantal} komende lessen krijgen {naar}.', { groep, aantal, naar })`.

    Zet er één regel uitleg onder, want dit is de zin die de belofte van deze fase draagt:
    `t('Lessen die al geweest zijn veranderen niet, en wie een les werkelijk gaf blijft staan zoals het staat.')`.

    Toon de sectie alleen als `plan.trainerwissels.length > 0`.

    Neem de trainerwissels op in `nietsNieuws`: een bestand dat alleen een trainerswissel bevat moet
    de knop Importeren aan zetten. Zonder dat is de hele fase op het scherm onbereikbaar.

    Werk de resultaatzin bij zodat het aantal bijgewerkte lessen erin staat, bijvoorbeeld:
    `t('{groepen} lesgroepen aangemaakt, {bijgewerkt} bijgewerkt, {spelers} spelers erbij, {lessen} lessen ingepland, {gewisseld} lessen kregen een andere trainer.', ...)`.
    Vervang de bestaande zin volledig in plaats van er een tweede naast te zetten — anders staat er
    één telling op twee plekken.

    Werk de tekst in het bevestigingsblok `Zeker weten?` bij: hierna staan niet alleen de spelers,
    de lesgroepen en de lessen echt in de app, maar krijgen ook de genoemde komende lessen hun
    nieuwe trainer.

    Reken niets uit op het scherm. `aantal` komt uit het plan; het scherm mag `boekingIds.length`
    niet zelf tellen — dat is de regel die het hele importpad volgt: het scherm toont wat `lib/`
    berekend heeft, zodat wat de beheerder leest hetzelfde is als wat er weggeschreven wordt.
  </action>

  <verify>
    <automated>cd "/Users/leko/Downloads/tennis app" && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "plan.trainerwissels" app/admin/trainingen-import.tsx` is minstens `2` (de voorwaarde en de lus).
    - `grep -n "nietsNieuws" -A 4 app/admin/trainingen-import.tsx` toont `trainerwissels` in de voorwaarde.
    - `grep -c "boekingIds.length" app/admin/trainingen-import.tsx` geeft `0` — het scherm telt niet zelf.
    - `grep -c "bijgewerkteLessen" app/admin/trainingen-import.tsx` is minstens `1`.
    - `npx tsc --noEmit` geeft geen enkele fout.
  </acceptance_criteria>

  <done>De droogloop toont per groep hoeveel komende lessen naar welke trainer gaan, en het resultaat telt ze achteraf.</done>
</task>

<task type="auto">
  <name>Taak 2: De Engelse kant, en de oplevering</name>
  <files>lib/i18n-en.ts, app/admin/trainingen-import.tsx</files>

  <read_first>
    - `lib/i18n-en.ts` — regels 1-40 (de regels van dit bestand) en het blok rond de bestaande importteksten (zoek op `lesgroepen aangemaakt`)
    - `app/admin/trainingen-import.tsx` — de zinnen die taak 1 opleverde
    - `lib/i18n.ts` — hoe `t(zin, vars)` de plaatshouders invult
  </read_first>

  <action>
    Voeg voor elke nieuwe of gewijzigde Nederlandse zin uit taak 1 een ingang toe aan `EN` in
    `lib/i18n-en.ts`, met de Nederlandse zin letterlijk als sleutel — inclusief hoofdletters,
    leestekens en plaatshouders. Verwijder de ingang van de resultaatzin die je verving; een
    sleutel die nergens meer voorkomt is dode tekst.

    Controleer dat elke plaatshouder aan beide kanten voorkomt. Een `{aantal}` dat in het Engels
    ontbreekt levert een zin op die de helft van het antwoord weglaat.

    Draai daarna de drie opleveringspoorten. Voeg geen pakket toe, draai geen SQL en maak geen
    verbinding met Supabase.
  </action>

  <verify>
    <automated>cd "/Users/leko/Downloads/tennis app" && npx tsc --noEmit && npm test && npx expo export --platform web</automated>
  </verify>

  <acceptance_criteria>
    - Voor elke nieuwe `t('...')` in `app/admin/trainingen-import.tsx` bestaat een sleutel in `lib/i18n-en.ts`: `grep -o "t('[^']*'" app/admin/trainingen-import.tsx | sed "s/t('//;s/'$//" | while read -r z; do grep -qF "'$z':" lib/i18n-en.ts || echo "ONTBREEKT: $z"; done` geeft geen enkele regel uitvoer.
    - `grep -c "lesgroepen krijgen een andere trainer" lib/i18n-en.ts` geeft `1`.
    - `npx tsc --noEmit`, `npm test` en `npx expo export --platform web` eindigen alle drie met exitcode 0.
    - `git diff --name-only` toont uitsluitend `app/admin/trainingen-import.tsx` en `lib/i18n-en.ts`.
  </acceptance_criteria>

  <done>Elke nieuwe zin staat in het Engels naast zijn Nederlandse origineel, en de drie poorten slagen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Taak 3: Met eigen ogen — een trainerswissel op het scherm</name>
  <files>app/admin/trainingen-import.tsx, lib/i18n-en.ts</files>

  <read_first>
    - `.planning/phases/05.1-groepssleutel/05.1-CONTEXT.md` — het scenario waar de gebruiker om vroeg
    - `.planning/phases/05-excel-import-van-trainingen/05-09-SUMMARY.md` — hoe het importscherm te bereiken is
    - `$HOME/.claude/projects/-Users-leko-Downloads-tennis-app/memory/dev-server-praat-met-echte-supabase.md`
  </read_first>

  <action>Pauzeer en laat de gebruiker de stappen onder `<how-to-verify>` zelf doorlopen. Voer zelf niets uit, importeer niets en raak `.env` niet aan; wacht op het resume-signaal.</action>

  <what-built>
    De import herkent een lesgroep aan weekdag + beginuur + baan, benoemt een nieuwe groep naar haar
    moment, en toont in de droogloop hoeveel komende lessen een andere trainer krijgen. Alle drie de
    opleveringspoorten slagen en 1463+ tests zijn groen, inclusief de acceptatie op `koen.xlsx`.
  </what-built>

  <how-to-verify>
    **Let op, dit staat in de projectgeheugens en geldt hier:** de dev-server praat met de échte
    Supabase van de club. Zet `.env` uit (hernoem hem tijdelijk) vóór je iets importeert, zodat deze
    controle op de mock-opslag draait en er niets in de productiedata van de club terechtkomt.

    1. Start met `npm run web` en meld je aan als beheerder.
    2. Ga naar Beheer → Trainingen importeren.
    3. Kies `koen.xlsx` (staat in de hoofdmap van het project). Controleer in de droogloop:
       - tien nieuwe lesgroepen;
       - hun namen zijn momenten, zoals `Woensdag 17:00` — géén `Groep 8`;
       - 42 nieuwe spelers;
       - nul lessen ingepland, met per groep de melding over de ontbrekende trainer en de
         ontbrekende baan, en verder niets.
    4. Maak in Beheer een traineraccount aan met de naam `Koen Leemans` en een baan. Lees
       `koen.xlsx` opnieuw in en importeer het echt.
    5. Maak een tweede traineraccount aan, bijvoorbeeld `Sofie Maes`.
    6. Maak een kopie van `koen.xlsx` waarin je in de kolom `Coach` overal `Leemans Koen` vervangt
       door `Maes Sofie`. Verder verandert er niets.
    7. Kies dat bestand. Controleer in de droogloop:
       - er komen **nul** nieuwe lesgroepen bij — dit is de kern van de fase;
       - de sectie "Deze lesgroepen krijgen een andere trainer" staat er, met per groep een aantal
         komende lessen en de naam van de nieuwe trainer;
       - er staat niets over lessen die verdwijnen.
    8. Importeer. Open daarna een lesgroep en een les van vorige week: de les uit het verleden moet
       nog steeds op de oude trainer staan, de komende lessen op de nieuwe.
    9. Zet `.env` terug zoals hij was.
  </how-to-verify>

  <acceptance_criteria>
    - De tien groepen heten naar hun moment en niet naar een nummer.
    - Een bestand dat alleen in de kolom `Coach` verschilt levert nul nieuwe lesgroepen op.
    - De droogloop noemt per groep een aantal komende lessen en de nieuwe trainer, vóór het importeren.
    - Een les van vorige week staat na het importeren nog steeds op de oude trainer.
    - `.env` staat na afloop terug zoals hij was.
  </acceptance_criteria>

  <resume-signal>Typ "goedgekeurd", of beschrijf wat er anders was dan hierboven staat.</resume-signal>

  <done>De gebruiker heeft de trainerswissel met eigen ogen als wijziging gezien, en niet als nieuwe groep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Beheerder → importscherm | Het scherm heeft al een `isAdmin`-grens uit fase 5; dit plan verandert daar niets aan. |
| `lib/import-trainingen.ts` → scherm | Het scherm toont wat `lib/` berekende en rekent zelf niets uit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-10 | Repudiation | de droogloop | mitigate | Het aantal gewisselde lessen staat vóór het importeren op het scherm en komt uit dezelfde bron als wat weggeschreven wordt (`plan.trainerwissels`), niet uit een tweede telling op het scherm. |
| T-05.1-11 | Information Disclosure | `app/admin/trainingen-import.tsx` | mitigate | De `isAdmin`-grens uit fase 5 blijft ongewijzigd; dit plan raakt de toegangscontrole niet. |
| T-05.1-12 | Tampering | de handmatige controle | mitigate | De controle draait met `.env` uit, op de mock-opslag; anders raakt een test de productiedata van de club. Staat expliciet in het checkpoint. |
| T-05.1-SC | Tampering | npm-installaties | mitigate | Dit plan installeert geen enkel pakket. |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm test`, `npx expo export --platform web` slagen.
- Elke nieuwe `t('...')` heeft een Engelse tegenhanger (zie de grep-lus in taak 2).
- `git diff --name-only` toont uitsluitend de twee bestanden van dit plan.
</verification>

<success_criteria>
De beheerder ziet vóór het importeren om hoeveel komende lessen een trainerswissel gaat en naar wie
ze gaan (IMP-13), en ziet dat een nieuwe lesgroep naar haar moment heet (IMP-12). Het scenario is
met de hand nagelopen op de mock-opslag.
</success_criteria>

<output>
Create `.planning/phases/05.1-groepssleutel/05.1-04-SUMMARY.md` when done
</output>
