---
phase: 05.1-groepssleutel
plan: 05
type: execute
wave: 5
depends_on: ["05.1-04"]
files_modified:
  - lib/types.ts
  - lib/import-trainingen.ts
  - lib/import-trainingen.test.ts
  - providers/SimpleDataProvider.tsx
  - app/admin/trainingen-import.tsx
  - lib/i18n-en.ts
  - .planning/IMPORT-SJABLOON.md
autonomous: false
requirements: [IMP-16, IMP-17]
must_haves:
  truths:
    - "Een andere trainer op komende lessen en een speler die uit een roster verdwijnt worden apart bevestigd, met een eigen vinkje."
    - "Zet de beheerder dat vinkje niet, dan gaat de rest van de import gewoon door: nieuwe groepen, nieuwe spelers en nieuwe lessen komen er zonder extra klik."
    - "Zonder dat vinkje blijft de trainer op de komende lessen staan zoals hij stond, en blijft een speler die het bestand niet meer kent gewoon in het roster."
    - "Gaat het bestand grotendeels over het verleden, dan staat dat bovenaan de droogloop met de periode van het bestand erin."
    - "Is er al eerder een seizoen ingelezen én zou dit bestand iets terugdraaien, dan staat dat er ook bij."
    - "De waarschuwing en het percentage worden in `lib/` uitgerekend; het scherm toont ze alleen."
  artifacts:
    - path: "lib/import-trainingen.ts"
      provides: "`bestandsperiode`, `ingrijpendeWijzigingen`, `importWaarschuwingen`, de keuze-parameter van `bouwImportWijziging`"
      contains: "ingrijpendeWijzigingen"
    - path: "lib/types.ts"
      provides: "`Settings.laatste_trainingen_import`"
      contains: "laatste_trainingen_import"
    - path: "app/admin/trainingen-import.tsx"
      provides: "De waarschuwing bovenaan en het aparte bevestigingsblok"
      contains: "ingrijpend"
  key_links:
    - from: "app/admin/trainingen-import.tsx"
      to: "lib/import-trainingen.ts importWaarschuwingen"
      via: "de banner bovenaan de droogloop, uitgerekend in lib"
      pattern: "importWaarschuwingen\\("
    - from: "app/admin/trainingen-import.tsx voerUit"
      to: "importeerTrainingen(plan, keuze)"
      via: "de aparte keuze reist mee naar de provider"
      pattern: "importeerTrainingen\\(plan,"
    - from: "providers/SimpleDataProvider.tsx"
      to: "bouwImportWijziging(plan, newId, keuze)"
      via: "één opbouwer, geen tweede code-pad"
      pattern: "bouwImportWijziging\\(plan, newId,"
    - from: "providers/SimpleDataProvider.tsx"
      to: "Settings.laatste_trainingen_import"
      via: "in dezelfde commit als de import zelf"
      pattern: "laatste_trainingen_import"
---

<objective>
Bescherming tegen de vergissing, niet tegen de bedoeling (D-18). Wat de import van bestaande
gegevens zou wegnemen of omzetten krijgt een eigen bevestiging; de rest van de import gaat gewoon
door. En bovenaan de droogloop komt een waarschuwing als het bestand grotendeels over het verleden
gaat, of als er al eerder een seizoen is ingelezen en dit bestand daar overheen zou schrijven.

Purpose: het bestand is een foto van het moment waarop het gemaakt is (D-18). `koen.xlsx` in
september inlezen, in januari op het groepsscherm de woensdaggroep aan een andere trainer geven, en
in maart datzelfde septemberbestand nog eens openen — dan zegt het bestand nog steeds `Leemans
Koen` en schuift de agenda stilletjes terug. Dezelfde beweging haalt het kind er weer uit dat in
januari aan het roster toegevoegd werd. De import is er om een seizoen in te laden; het onderhoud
gebeurt in de app (D-13), en waar die twee elkaar tegenspreken hoort de app te remmen.
Output: twee pure rekenfuncties met tests, een keuze die door de opbouwer en de provider reist, een
banner en een apart bevestigingsblok op het scherm, en één handmatige controle van de val zelf.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/IMPORT-SJABLOON.md
@.planning/phases/05.1-groepssleutel/05.1-CONTEXT.md
@.planning/phases/05-excel-import-van-trainingen/05-08-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Wat de uitvoerder nodig heeft; geen zoektocht door de codebase. -->

Na plan 05.1-03 en 05.1-04 bestaat in `lib/import-trainingen.ts`:
```ts
export interface TrainerWissel {
  groep: string; van: string; naar: string; trainerId: string;
  aantal: number; boekingIds: string[];
}
export interface GroepInPlan {
  groep; status; naam; weekdag; beginuur; beginminuut; trainerNaam; trainer; baan;
  aantalSpelers; roster: string[]; toegevoegd: string[]; verwijderd: string[]; wijzigingen;
}
export interface ImportPlanLessen {
  regels: LesRegel[];
  groepenNieuw: GroepInPlan[]; groepenBijgewerkt: GroepInPlan[]; groepenOngewijzigd: GroepInPlan[];
  spelersNieuw; nieuweLessen; ongewijzigdeLessen; overgeslagen; handmatigGewijzigd;
  verdwenenUitBestand; trainerwissels: TrainerWissel[]; fouten; waarschuwingen; nietHerkend; dubbel;
}
export interface ImportWijziging {
  nieuweUsers: User[]; nieuweGroepen: LesGroep[]; gewijzigdeGroepen: GroepBijwerking[];
  nieuweBoekingen: Booking[];
  gewijzigdeBoekingen: Array<{ id: string; patch: { coach_id: string } }>;
  fouten: ImportFoutLessen[];
}
export function bouwImportWijziging(plan: ImportPlanLessen, maakId: (v: string) => string): ImportWijziging
export function planImportLessen(rijen, bestaandeGroepen, users, courts, bookings,
  settings: Pick<Settings, 'lesson_duration_minutes' | 'vakanties'>, nu: Date): ImportPlanLessen
```

Binnen `bouwImportWijziging` staan de twee plekken die dit plan aanraakt:
```ts
for (const inPlan of plan.groepenBijgewerkt) {
  const bestaand = inPlan.groep.bestaand;
  if (!bestaand) continue;
  idVanGroep.set(inPlan.groep, bestaand.id);
  uit.gewijzigdeGroepen.push({
    id: bestaand.id,
    patch: { ...inPlan.wijzigingen, roster: echteIds(inPlan.roster) },
  });
}
```
en de lus die `plan.trainerwissels` in `uit.gewijzigdeBoekingen` legt (plan 05.1-03, taak 2).

`LesRegel` heeft `datum: { jaar: number; maand: number; dag: number }` en
`uur: { uur: number; minuut: number }`. `dagSleutel(d: Date)` uit `lib/vakanties` geeft
`jjjj-mm-dd` uit lokale velden.

**`club_settings` is al bereikbaar en heeft geen migratie nodig — nagekeken:**
`supabase-schema.sql` regel 219-223 definieert `club_settings (id text primary key, value jsonb not
null default '{}'::jsonb, updated_at timestamptz)` — één rij, met de hele `Settings` als **jsonb**.
`providers/supabaseStore.ts` regel 299-304 doet
`supabase.from('club_settings').upsert({ id: 'club', value: change.settings, ... })`. Een veld
toevoegen aan `interface Settings` in `lib/types.ts` bereikt dus de opslag zonder één regel SQL en
zonder `alter table`. `planImportLessen` krijgt `settings` al binnen, en `importeerTrainingen` in de
provider heeft `storeRef.current.settings` en schrijft met `commit({ ...store, settings })` — dus
zowel de leeskant als de schrijfkant is er al.

`app/admin/trainingen-import.tsx`:
- regel 97: `const { users, courts, bookings, lesGroepen, settings, importeerTrainingen } = useSimpleData();`
- regel 174-176: `setPlan(planImportLessen(blad.rijen, lesGroepen, users, courts, bookings, settings, new Date()));`
- regel 195-206: `voerUit()` doet `meldImportKlaar(await importeerTrainingen(plan));`
- regel 522-530: het bevestigingsblok `Zeker weten?` met `Ja, nu importeren` / `Nee, toch niet`
- plan 05.1-04 voegde daar de sectie `Deze lesgroepen krijgen een andere trainer` aan toe.

`providers/SimpleDataProvider.tsx` regel 239: `importeerTrainingen: (plan: ImportPlanLessen) => Promise<ImportUitslagLessen>;`
</interfaces>

<placement_note>
**Waarom dit een eigen plan is en niet een taak in 05.1-04.** Plan 04 zet de droogloopsectie neer
die de trainerswissel *toont* en sluit af met een checkpoint dat precies dát bevestigt. Dit plan zet
er de *rem* op: een aparte bevestiging, een banner erboven, een nieuw veld in `Settings` en een
keuze die tot in de provider doorreist. Dat is nieuw `lib/`-rekenwerk plus een provider plus een
scherm — samen met plan 04 in één plan zou dat zes taken en twee checkpoints worden, ruim over het
contextbudget van één uitvoerder. Bovendien gaat elk checkpoint zo over één ding: 04 vraagt "zie je
de wissel?", 05 vraagt "houdt de rem?". De golf wordt 5, na 04: beide plannen schrijven in
`app/admin/trainingen-import.tsx` en `lib/i18n-en.ts`, dus ze kunnen sowieso niet naast elkaar.
</placement_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: Het rekenwerk — de periode van het bestand en wat er teruggedraaid zou worden</name>
  <files>lib/types.ts, lib/import-trainingen.ts, lib/import-trainingen.test.ts</files>

  <read_first>
    - `lib/import-trainingen.ts` — regels 300-440 (`LesRegel`, `leesLesRegels`), voor de vorm van een regel met `datum` en `uur`
    - `lib/import-trainingen.ts` — `ImportPlanLessen`, `GroepInPlan` en `planImportLessen` (regels ~1400-1560), plus `TrainerWissel` uit plan 05.1-03
    - `lib/import-trainingen.ts` — `overgeslagenPerReden` en `geweigerdeNieuweGroepen`, als voorbeeld van een pure afleiding over het plan die het scherm alleen maar toont
    - `lib/types.ts` — regels 464-485 (`interface Settings`), met het commentaar bij `vakanties` en `lesson_duration_minutes` als voorbeeld van hoe een optioneel veld hier gedocumenteerd wordt
    - `lib/vakanties.ts` — `dagSleutel`
    - `.planning/phases/05.1-groepssleutel/05.1-CONTEXT.md` — D-16, D-17, D-18
  </read_first>

  <behavior>
    `bestandsperiode(regels, nu)`:
    - Een bestand van 9 september 2026 tot 25 juni 2027, gelezen op 1 januari 2027, geeft `van: '2026-09-09'`, `tot: '2027-06-25'` en een `aandeelGeweest` van ongeveer 0,4.
    - Gelezen op een dag ná `tot` geeft het `aandeelGeweest: 1`.
    - Gelezen op een dag vóór `van` geeft het `aandeelGeweest: 0`.
    - Eén regel — `van` is gelijk aan `tot` — geeft geen deling door nul: vóór die dag `0`, erna `1`.
    - Nul regels geeft `null`; er is dan geen periode om iets over te zeggen.

    `ingrijpendeWijzigingen(plan, users)`:
    - Een plan met één trainerwissel van drie lessen levert die wissel op en `aantalLessen: 3`.
    - Een bijgewerkte groep waaruit één speler verdwijnt, levert één ingang met de groepsnaam en de naam van die speler.
    - Een groep waar alleen iemand bíj komt levert niets op — erbij komen neemt niets weg.
    - Een nieuwe groep levert nooit iets op: er is nog niets om terug te draaien.
    - Een leeg resultaat is herkenbaar aan `aantalLessen === 0 && spelersEruit.length === 0`.

    `importWaarschuwingen(plan, periode, ingrijpend, settings)`:
    - Is `aandeelGeweest >= 0.5`, dan zit er een waarschuwing `'verleden'` bij, met `van`, `tot` en een afgerond percentage in `vars`.
    - Is `settings.laatste_trainingen_import` gevuld én is `ingrijpend` niet leeg, dan zit er een waarschuwing `'sindsdien-gewijzigd'` bij, met de datum van die vorige import erin.
    - Is er nooit eerder geïmporteerd, dan komt die tweede waarschuwing er niet — er is dan niets om overheen te schrijven.
    - Een vers bestand over een komend seizoen, zonder ingrijpende wijzigingen, levert een lege lijst op.
  </behavior>

  <action>
    **Het veld in `Settings`.** Voeg `laatste_trainingen_import?: string` toe aan `interface Settings`
    in `lib/types.ts`, als ISO-tijdstip. Zet er een Nederlands commentaar bij dat drie dingen zegt:
    waarvoor het dient (weten dát er al eens een seizoen ingelezen is, zodat de droogloop kan
    waarschuwen dat een ouder bestand er overheen zou schrijven); dat het optioneel is en dat
    "afwezig" gewoon "nog nooit geïmporteerd" betekent, precies zoals `vakanties` en
    `lesson_duration_minutes` zich gedragen; en waarom dit géén migratie vraagt — `club_settings`
    bewaart de hele `Settings` als één `jsonb`-waarde, dus een veld erbij bereikt de opslag zonder
    `alter table`.

    Zet er ook bij wat dit veld **niet** is, want dat is de eerlijkheid die deze fase nodig heeft:
    het is geen wijzigingslogboek. De app kan niet zien wát er sinds die datum in de app veranderd
    is — er is geen `updated_at` per roster. Wat de waarschuwing daarom zegt, is precies wat wél
    waar is: er is eerder een seizoen ingelezen, én dit bestand zou nu iets terugdraaien. Het
    concrete bewijs staat eronder in het bevestigingsblok, met namen en aantallen erbij.

    **`bestandsperiode`.** Voeg toe:
    `export interface Bestandsperiode { van: string; tot: string; aandeelGeweest: number; }` en
    `export function bestandsperiode(regels: readonly LesRegel[], nu: Date): Bestandsperiode | null`.
    `van` en `tot` zijn dagsleutels (`jjjj-mm-dd`) uit de vroegste en laatste datum in het bestand,
    met lokale datumvelden gebouwd — nooit een ISO-tekst laten parsen, want dan schuift de dag in
    een westelijke tijdzone. `aandeelGeweest` is het deel van de periode `van`–`tot` dat vóór `nu`
    ligt, geklemd op 0 tot 1.

    Reken over de **periode** en niet over het aantal lessen, en zet dat als commentaar erbij: D-17
    schrijft de zin letterlijk voor — "Dit bestand gaat over 9 september 2026 tot 25 juni 2027, en
    60% daarvan is al geweest" — en "daarvan" slaat op de periode. Een percentage over de regels zou
    een ander getal geven dan de zin belooft, en zou bovendien meebewegen met hoeveel leerlingen er
    per les in het bestand staan; dat is geen eigenschap van de tijd.

    **`ingrijpendeWijzigingen`.** Voeg toe:
    ```
    export interface SpelerEruit { groep: string; namen: string[]; ids: string[]; }
    export interface IngrijpendeWijzigingen {
      trainerwissels: TrainerWissel[];
      spelersEruit: SpelerEruit[];
      aantalLessen: number;
    }
    export function ingrijpendeWijzigingen(
      plan: ImportPlanLessen, users: readonly User[],
    ): IngrijpendeWijzigingen
    ```
    Ze **selecteert en rekent niets opnieuw uit**: `trainerwissels` is `plan.trainerwissels`
    ongewijzigd, `aantalLessen` is de som van hun `aantal`, en `spelersEruit` komt uit
    `plan.groepenBijgewerkt` — per groep met een niet-lege `verwijderd`, met de ids en de namen
    daarbij opgezocht in `users`. Zet dat als commentaar erbij: dit is een filter over wat de
    droogloop al berekend heeft, geen tweede weg naar hetzelfde antwoord. Zou hier iets herrekend
    worden, dan kan het bevestigingsblok iets anders tonen dan wat er straks wel of niet gebeurt —
    precies de belofte die `bouwImportWijziging` al bewaakt.

    Neem `plan.verdwenenUitBestand` hier **niet** in op, en schrijf erbij waarom: een les die uit
    het bestand verdween is al een melding en géén opdracht — de import verwijdert nooit een les
    (D-13 van fase 5). Er valt dus niets aan te bevestigen.

    **`importWaarschuwingen`.** Voeg toe:
    ```
    export type ImportWaarschuwingSoort = 'verleden' | 'sindsdien-gewijzigd';
    export interface ImportWaarschuwing {
      soort: ImportWaarschuwingSoort;
      reden: string;
      vars: Record<string, string | number>;
    }
    export function importWaarschuwingen(
      periode: Bestandsperiode | null,
      ingrijpend: IngrijpendeWijzigingen,
      settings: Pick<Settings, 'laatste_trainingen_import'>,
    ): ImportWaarschuwing[]
    ```
    `reden` is een vaste zin met plaatshouders, nooit tekst met de waarde er al in geplakt —
    dezelfde regel als `ImportFoutLessen` hierboven, zodat het scherm `t(reden, vars)` kan doen en
    de zin vertaalbaar blijft. Gebruik voor `'verleden'` de zin uit D-17:
    `'Dit bestand gaat over {van} tot {tot}, en {percentage}% daarvan is al geweest.'` Rond het
    percentage af op een heel getal. Voor `'sindsdien-gewijzigd'`:
    `'Je las al eerder een seizoen in op {datum}. Dit bestand draait terug wat je daarna in de app wijzigde — kijk hieronder na wat dat precies is.'`

    Roep in `planImportLessen` niets van dit alles aan: dit zijn afleidingen over een klaar plan,
    net als `overgeslagenPerReden`. Wel: verbreed de `settings`-parameter van `planImportLessen`
    niet — het scherm geeft `settings` al in zijn geheel mee en `importWaarschuwingen` krijgt hem
    apart.

    Schrijf de tests uit `<behavior>` in `lib/import-trainingen.test.ts`, in drie eigen
    `describe`-blokken. Gebruik een vaste `nu` als parameter, nooit `new Date()` in de test.
  </action>

  <verify>
    <automated>cd "/Users/leko/Downloads/tennis app" && npx jest lib/import-trainingen.test.ts -t "bestandsperiode" && npx jest lib/import-trainingen.test.ts -t "ingrijpendeWijzigingen" && npx jest lib/import-trainingen.test.ts -t "importWaarschuwingen"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "export function bestandsperiode\|export function ingrijpendeWijzigingen\|export function importWaarschuwingen" lib/import-trainingen.ts` geeft `3`.
    - `grep -n "laatste_trainingen_import" lib/types.ts` geeft precies één treffer, met een doc-commentaar erboven dat het woord `jsonb` bevat.
    - `git diff -- supabase-schema.sql | wc -l` geeft `0` — geen migratie, geen SQL.
    - `sed -n '/export function ingrijpendeWijzigingen/,/^}/p' lib/import-trainingen.ts | grep -c "verdwenenUitBestand"` geeft `0`.
    - `sed -n '/export function bestandsperiode/,/^}/p' lib/import-trainingen.ts | grep -c "new Date()"` geeft `0` — `nu` komt binnen als parameter.
    - De drie `-t`-runs slagen, samen met minstens veertien tests (de gevallen uit `<behavior>`).
  </acceptance_criteria>

  <done>De periode van het bestand, het percentage verleden en de lijst van wat teruggedraaid zou worden staan als pure, geteste functies in `lib/`, en `Settings` onthoudt wanneer er voor het laatst geïmporteerd is — zonder één regel SQL.</done>
</task>

<task type="auto" tdd="true">
  <name>Taak 2: De keuze reist mee — selectief toepassen in de opbouwer en in de provider</name>
  <files>lib/import-trainingen.ts, lib/import-trainingen.test.ts, providers/SimpleDataProvider.tsx</files>

  <read_first>
    - `lib/import-trainingen.ts` — `bouwImportWijziging` in zijn geheel, in het bijzonder `echteIds`, de lus over `plan.groepenBijgewerkt` en de lus die `gewijzigdeBoekingen` vult (plan 05.1-03)
    - `lib/import-trainingen.ts` — `ImportWijziging` en `ImportUitslagLessen`
    - `lib/import-trainingen.test.ts` — `describe('bouwImportWijziging')` in zijn geheel, met het hulpje `teller()` en de determinismetest
    - `providers/SimpleDataProvider.tsx` — regels 1185-1260: het doc-commentaar en `importeerTrainingen`, inclusief de twee patch-`Map`s
    - `providers/SimpleDataProvider.tsx` — regel 239 (de typedeclaratie van `importeerTrainingen` in de contextinterface) en `saveSettings` rond regel 1396, als voorbeeld van hoe `settings` in een `commit` meegaat
  </read_first>

  <behavior>
    - Met `{ ingrijpend: true }` levert `bouwImportWijziging` exact hetzelfde op als vandaag: de trainerpatches staan erin en een verwijderde speler staat niet meer in het roster.
    - Met `{ ingrijpend: false }` is `gewijzigdeBoekingen` leeg.
    - Met `{ ingrijpend: false }` bevat het roster van een bijgewerkte groep nog steeds de speler die het bestand niet meer kent, én de speler die erbij komt — erbij komen gaat altijd door.
    - Met `{ ingrijpend: false }` komen nieuwe groepen, nieuwe spelers en nieuwe lessen er onverkort bij: er wordt niets anders geremd.
    - Verandert een bijgewerkte groep alléén doordat er een speler uit gaat, dan levert `{ ingrijpend: false }` voor die groep een patch op die niets wegneemt.
    - Twee keer dezelfde invoer met dezelfde keuze geeft twee keer exact dezelfde rijen.
  </behavior>

  <action>
    Voeg toe: `export interface ImportKeuze { ingrijpend: boolean; }` en geef
    `bouwImportWijziging` een derde parameter `keuze: ImportKeuze` — **verplicht, zonder
    standaardwaarde**. Schrijf in het commentaar waarom er geen standaard is: dit is dezelfde
    discipline als `nu` overal in dit importpad. Een standaard `true` zou betekenen dat een caller
    die vergeet te kiezen stilzwijgend de agenda omzet; een standaard `false` zou betekenen dat een
    caller die vergeet te kiezen stilzwijgend de bevestiging van de beheerder negeert. Beide zijn
    erger dan een compilefout. Werk de bestaande aanroepen in de tests mee bij.

    Pas de keuze op precies twee plekken toe, in de bestaande lussen — geen tweede code-pad:
    1. In de lus over `plan.groepenBijgewerkt`: is `keuze.ingrijpend` onwaar, bouw het roster dan
       uit `inPlan.roster` **plus** `inPlan.verwijderd`, zodat wie eruit zou vallen blijft staan.
       Wie erbij komt zit al in `inPlan.roster` en gaat dus altijd door.
    2. In de lus die `plan.trainerwissels` omzet: is `keuze.ingrijpend` onwaar, sla hem dan over,
       zodat `gewijzigdeBoekingen` leeg blijft.

    Zet boven die twee plekken één gedeeld commentaar dat de asymmetrie verantwoordt: erbij komen
    is nooit ingrijpend en wordt nooit geremd; wegnemen en omzetten wel (D-16). De gewone weg — een
    seizoen inladen, groepen aanmaken, spelers aanmaken, lessen inplannen — blijft één klik, want
    een rem die overal staat is een rem die niemand meer leest.

    Werk het doc-commentaar van `bouwImportWijziging` bij: de belofte "er wordt hier niets opnieuw
    uitgerekend" blijft overeind — de keuze laat weg, ze berekent niets anders.

    **De provider.** Geef `importeerTrainingen` een tweede parameter `keuze: ImportKeuze`, ook in de
    typedeclaratie van de contextinterface (regel 239), en geef hem door aan `bouwImportWijziging`.
    Schrijf in dezelfde `commit` ook `settings: { ...store.settings, laatste_trainingen_import: new Date().toISOString() }`
    weg. Zet erbij waarom dat hier hoort en niet in `lib/`: `lib/import-trainingen.ts` mag geen klok
    lezen — dat is wat het hele plan testbaar houdt — en de provider is al de plek waar `newId` en
    de tijd vandaan komen.

    Werk het doc-commentaar boven `importeerTrainingen` bij met wat er nu geldt: de import werkt
    ook bestaande boekingen bij en kan een speler uit een roster halen, maar alleen als de
    beheerder dat apart bevestigde; verwijderen doet ze nog steeds nooit.

    Breid `describe('bouwImportWijziging')` uit met de gevallen uit `<behavior>`, inclusief de
    determinismetest met beide keuzes.
  </action>

  <verify>
    <automated>cd "/Users/leko/Downloads/tennis app" && npx jest lib/import-trainingen.test.ts && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "export interface ImportKeuze" lib/import-trainingen.ts` geeft precies één treffer.
    - `grep -n "export function bouwImportWijziging" -A 4 lib/import-trainingen.ts` toont `keuze: ImportKeuze` zonder `=` erachter (geen standaardwaarde).
    - `sed -n '/export function bouwImportWijziging/,/^}/p' lib/import-trainingen.ts | grep -c "keuze.ingrijpend"` geeft `2` — precies twee plekken, geen tweede code-pad.
    - `sed -n '/const importeerTrainingen/,/}, \[commit\]);/p' providers/SimpleDataProvider.tsx | grep -c "laatste_trainingen_import"` geeft `1`.
    - `sed -n '/const importeerTrainingen/,/}, \[commit\]);/p' providers/SimpleDataProvider.tsx | grep -c "filter\|splice"` geeft `0` — er wordt nog steeds niets weggehaald.
    - `grep -v '^\s*[*/]' lib/import-trainingen.ts | grep -c "new Date()"` geeft `0` — `lib/` leest geen klok.
    - `npx jest lib/import-trainingen.test.ts` slaagt; `npx tsc --noEmit` geeft geen fout.
  </acceptance_criteria>

  <done>Eén opbouwer met één extra keuze remt precies de twee ingrijpende dingen en laat de rest van de import ongemoeid; de provider onthoudt wanneer er geïmporteerd is.</done>
</task>

<task type="auto">
  <name>Taak 3: De waarschuwing bovenaan, het aparte vinkje, de Engelse teksten en het sjabloon</name>
  <files>app/admin/trainingen-import.tsx, lib/i18n-en.ts, .planning/IMPORT-SJABLOON.md</files>

  <read_first>
    - `app/admin/trainingen-import.tsx` — regels 93-215: de schermstaat, `toonPlan`, `voerUit`, `groepRegel`
    - `app/admin/trainingen-import.tsx` — regels 330-470: de droogloop, met de kaart `Deze lesgroepen krijgen een andere trainer` uit plan 05.1-04 en de vorm van de waarschuwingskaarten eronder
    - `app/admin/trainingen-import.tsx` — regels 470-560: het resultaat, het bevestigingsblok `Zeker weten?` en de knop `Importeren` met zijn `disabled={nietsNieuws}`
    - `components/` — zoek een bestaande vinkje-/schakelaarcomponent (`grep -rln "Switch\|Checkbox\|Vinkje" components`) en gebruik die in plaats van er een nieuwe te maken; is er geen, gebruik dan een `Pressable` in de stijl van de bestaande knoppen
    - `lib/i18n-en.ts` — regels 1-40 en de bestaande importzinnen
    - `.planning/IMPORT-SJABLOON.md` — de sectie `## De droogloop` en de sectie `## Herimport`
    - `lib/import-trainingen.ts` — `bestandsperiode`, `ingrijpendeWijzigingen`, `importWaarschuwingen`, `ImportKeuze` uit taak 1 en 2
  </read_first>

  <action>
    **De banner.** Reken bovenaan de droogloopweergave, met `useMemo` over het plan, de drie
    afleidingen uit: `bestandsperiode(plan.regels, nu)`, `ingrijpendeWijzigingen(plan, users)` en
    `importWaarschuwingen(periode, ingrijpend, settings)`. Toon elke waarschuwing als eigen regel,
    bóven de kop `Dit gaat er gebeuren`, in de opvallendste van de bestaande waarschuwingsstijlen —
    dit is het eerste wat iemand moet zien en niet iets onderaan (D-15). Toon ze met
    `t(w.reden, w.vars)`. Bewaar de `nu` die je aan `planImportLessen` gaf in de schermstaat en geef
    diezelfde waarde aan `bestandsperiode`, zodat de banner en het plan het over hetzelfde moment
    hebben.

    **Het aparte vinkje.** Voeg onder de bestaande groepen- en wisselkaarten een eigen blok toe,
    zichtbaar zodra `ingrijpend.aantalLessen > 0 || ingrijpend.spelersEruit.length > 0`. Kop:
    `t('Dit neemt iets weg — bevestig apart')`. Eronder wat er precies zou gebeuren:
    - per trainerwissel `t('{groep}: {aantal} komende lessen gaan naar {naar}.', ...)`;
    - per groep met vertrekkers `t('{groep}: {namen} gaan uit het roster.', { groep, namen })` — plak
      de namen met `', '` aan elkaar in het scherm; dat is opmaak, geen rekenwerk.
    Zet er de uitleg onder die de val benoemt, want dat is waar dit blok voor bestaat:
    `t('Een importbestand is een foto van het moment waarop het gemaakt is. Een ouder bestand zet terug wat je daarna in de app wijzigde. Laat dit uit als je alleen lessen wil bijladen.')`

    Daaronder één vinkje, standaard **uit**:
    `t('Ja, pas ook deze wijzigingen toe')`. De schermstaat is één `boolean`; geef hem als
    `{ ingrijpend }` mee aan `importeerTrainingen(plan, { ingrijpend })` in `voerUit`. Standaard uit
    is de kern van IMP-16: wie doorklikt zonder te lezen, doet niets onomkeerbaars.

    Werk de tekst van het bevestigingsblok `Zeker weten?` bij zodat ze zegt wat er nu gebeurt, en
    laat haar meebewegen met het vinkje: staat het aan, dan hoort er te staan dat ook de genoemde
    lessen en roosters mee veranderen; staat het uit, dan dat die met rust gelaten worden. Twee
    zinnen, allebei door `t()`.

    Reken op het scherm niets uit. Geen percentage, geen aantal, geen datumbereik: alles komt uit
    de drie functies van taak 1. Het scherm mag namen aan elkaar plakken en een lijst renderen,
    verder niets.

    **Het sjabloon.** Werk `.planning/IMPORT-SJABLOON.md` bij — het is bindend en zegt vandaag niet
    wat de droogloop nu doet. Voeg aan `## De droogloop` twee punten toe: de waarschuwing bovenaan
    met de periode van het bestand, en de aparte bevestiging van wat er weggenomen of omgezet wordt.
    Vul `## Herimport` aan met de reden in twee zinnen (D-18): het bestand is een foto van zijn
    eigen moment, en de app remt op de vergissing en niet op de bedoeling.

    **De Engelse kant.** Voeg voor elke nieuwe Nederlandse zin een ingang toe aan `EN` in
    `lib/i18n-en.ts`, met de Nederlandse zin letterlijk als sleutel en met dezelfde plaatshouders
    aan beide kanten. Verwijder de sleutel van elke zin die je verving.

    Draai daarna de drie opleveringspoorten. Geen pakket, geen SQL, geen Supabase-verbinding.
  </action>

  <verify>
    <automated>cd "/Users/leko/Downloads/tennis app" && npx tsc --noEmit && npm test && npx expo export --platform web</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "importWaarschuwingen\|ingrijpendeWijzigingen\|bestandsperiode" app/admin/trainingen-import.tsx` is minstens `3`.
    - `grep -n "importeerTrainingen(plan," app/admin/trainingen-import.tsx` toont dat de keuze meegegeven wordt.
    - Het vinkje staat standaard uit: `grep -n "useState<boolean>(false)" app/admin/trainingen-import.tsx` toont de nieuwe staat.
    - Het scherm rekent niet: `grep -c "Math.round\|/ 100\|\* 100\|getTime()" app/admin/trainingen-import.tsx` geeft `0`.
    - Elke `t('...')` in het scherm heeft een Engelse tegenhanger: `grep -o "t('[^']*'" app/admin/trainingen-import.tsx | sed "s/t('//;s/'$//" | while read -r z; do grep -qF "'$z':" lib/i18n-en.ts || echo "ONTBREEKT: $z"; done` geeft geen uitvoer.
    - `grep -c "foto van het moment" .planning/IMPORT-SJABLOON.md` is minstens `1`.
    - `npx tsc --noEmit`, `npm test` en `npx expo export --platform web` eindigen alle drie met exitcode 0.
    - `git diff --name-only | grep -c "package.json\|package-lock.json\|supabase-schema.sql"` geeft `0`.
  </acceptance_criteria>

  <done>De droogloop opent met de waarschuwing en de periode, het wegnemen zit achter een eigen vinkje dat standaard uit staat, de rest van de import is één klik, en het bindende sjabloon klopt weer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Taak 4: De val zelf — september inlezen, in januari wijzigen, in maart terugkomen</name>
  <files>app/admin/trainingen-import.tsx</files>

  <read_first>
    - `.planning/phases/05.1-groepssleutel/05.1-CONTEXT.md` — D-15 tot en met D-18
    - `.planning/phases/05-excel-import-van-trainingen/05-09-SUMMARY.md` — hoe het importscherm te bereiken is
    - `$HOME/.claude/projects/-Users-leko-Downloads-tennis-app/memory/dev-server-praat-met-echte-supabase.md`
  </read_first>

  <action>Pauzeer en laat de gebruiker de stappen onder `<how-to-verify>` zelf doorlopen. Voer zelf niets uit, importeer niets en raak `.env` niet aan; wacht op het resume-signaal.</action>

  <what-built>
    Wat de import van bestaande gegevens zou wegnemen of omzetten — een andere trainer op komende
    lessen, en een speler die uit een roster verdwijnt — staat nu in een eigen blok met een vinkje
    dat standaard uit staat. Bovenaan de droogloop staat de periode van het bestand met het
    percentage dat al geweest is, en de melding dat er al eerder een seizoen ingelezen werd. Alle
    drie de opleveringspoorten slagen.
  </what-built>

  <how-to-verify>
    **Zet eerst `.env` uit** (hernoem hem tijdelijk). De dev-server praat met de échte Supabase van
    de club, en deze controle mag daar niet in landen. Zet hem na afloop terug.

    Speel de val van D-18 na, in deze volgorde:

    1. Start met `npm run web`, meld je aan als beheerder, en zorg dat er twee traineraccounts zijn
       (`Koen Leemans` en bijvoorbeeld `Sofie Maes`) en één baan.
    2. Beheer → Trainingen importeren → kies `koen.xlsx` en importeer het echt. Dit is "september".
    3. **De januari-wijziging, met de hand in de app.** Open de lesgroep van woensdag 17:00:
       - geef hem `Sofie Maes` als trainer, via het groepsscherm;
       - voeg één speler aan het roster toe die niet in `koen.xlsx` staat.
    4. **De maart-vergissing.** Kies in het importscherm opnieuw exact hetzelfde `koen.xlsx`.
       Controleer, vóór je iets aanklikt:
       - bovenaan staat een waarschuwing met de periode van het bestand en een percentage dat al
         geweest is, in gewone taal;
       - er staat ook een zin dat je al eerder een seizoen inlas, met de datum erbij;
       - er is een apart blok "Dit neemt iets weg — bevestig apart", met daarin de trainerwissel
         terug naar `Leemans Koen` mét een aantal komende lessen, én de speler die je in stap 3
         toevoegde en die er nu weer uit zou gaan;
       - het vinkje bij dat blok staat **uit**.
    5. Importeer zonder het vinkje aan te zetten. Controleer daarna:
       - de groep heeft nog steeds `Sofie Maes` als trainer;
       - de in stap 3 toegevoegde speler zit nog steeds in het roster;
       - er is niets stukgegaan: de import meldt gewoon dat er niets nieuws bij kwam.
    6. Kies hetzelfde bestand nog eens, zet nu het vinkje wél aan, en importeer. Controleer:
       - de komende lessen staan weer op `Leemans Koen`;
       - de toegevoegde speler is uit het roster;
       - een les van vóór vandaag staat nog steeds op de trainer die er stond, en wie hem
         werkelijk gaf is niet veranderd.
    7. Zet `.env` terug zoals hij was.
  </how-to-verify>

  <acceptance_criteria>
    - De waarschuwing bovenaan noemt de periode van het bestand en een percentage, in gewone taal.
    - De melding over de vorige import verschijnt pas nádat er één keer geïmporteerd is.
    - Het aparte blok noemt zowel de trainerwissel (met een aantal) als de speler die eruit zou gaan (met naam).
    - Het vinkje staat standaard uit, en importeren zonder dat vinkje laat trainer én roster ongemoeid.
    - Met het vinkje aan gebeurt precies wat het blok aankondigde, en geen les uit het verleden verandert.
    - `.env` staat na afloop terug zoals hij was.
  </acceptance_criteria>

  <resume-signal>Typ "goedgekeurd", of beschrijf wat er anders was dan hierboven staat.</resume-signal>

  <done>De vergissing waar de gebruiker om vroeg is met eigen ogen tegengehouden, en met het vinkje aan alsnog bewust uitgevoerd.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Excel-bestand → droogloop | Een oud bestand is geen aanval maar een vergissing; het effect op bestaande data is hetzelfde. |
| Beheerder → `importeerTrainingen(plan, keuze)` | De keuze is de grens: zonder expliciete bevestiging wordt er niets weggenomen of omgezet. |
| `Settings` → `club_settings.value` (jsonb) | Eén rij, admin-only schrijfbaar via de bestaande RLS-policy `club_settings_write`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-13 | Tampering | een oud bestand dat de agenda terugzet | mitigate | `bouwImportWijziging` past trainerwissels en rosterverwijderingen alleen toe bij `keuze.ingrijpend`; het vinkje op het scherm staat standaard uit. Getest in taak 2 en met de hand in taak 4. |
| T-05.1-14 | Repudiation | "ik wist niet dat dat zou gebeuren" | mitigate | De banner noemt de periode en het percentage verleden; het aparte blok noemt groep, aantal lessen en de namen van de vertrekkers — allemaal uit dezelfde bron als wat weggeschreven wordt. |
| T-05.1-15 | Elevation of Privilege | `club_settings` schrijven | accept | Het nieuwe veld reist mee in de bestaande `Settings`-upsert, onder de bestaande admin-only policy `club_settings_write` (supabase-schema.sql regel 692-695). Geen nieuwe tabel, geen nieuwe policy, geen migratie. |
| T-05.1-16 | Information Disclosure | de namen van de vertrekkers op het scherm | accept | Het importscherm zit al achter de `isAdmin`-grens uit fase 5; een beheerder ziet die namen sowieso op het groepsscherm. |
| T-05.1-17 | Tampering | de handmatige controle | mitigate | De controle draait met `.env` uit, op de mock-opslag; het checkpoint zegt het als eerste en als laatste stap. |
| T-05.1-SC | Tampering | npm-installaties | mitigate | Dit plan installeert geen enkel pakket; `git diff --name-only` mag `package.json` niet tonen. |
</threat_model>

<verification>
- `npx jest lib/import-trainingen.test.ts` slaagt.
- `npx tsc --noEmit`, `npm test`, `npx expo export --platform web` slagen.
- `git diff -- supabase-schema.sql | wc -l` geeft `0` — geen SQL, geen migratie.
- `grep -c "jest.mock\|jest.fn" lib/import-trainingen.test.ts` geeft `0`.
- `grep -c "from '../providers\|from '../components\|from '../app" lib/import-trainingen.ts` geeft `0`.
- `grep -v '^\s*[*/]' lib/import-trainingen.ts | grep -c "new Date()"` geeft `0`.
</verification>

<success_criteria>
Een trainerswissel op komende lessen en een speler die uit een roster verdwijnt worden apart
bevestigd, los van de rest van de import, die gewoon doorgaat (IMP-16). Een bestand dat grotendeels
over het verleden gaat, of een app die sinds de vorige import gewijzigd is, levert bovenaan de
droogloop een waarschuwing met de periode van het bestand erbij (IMP-17).
</success_criteria>

<output>
Create `.planning/phases/05.1-groepssleutel/05.1-05-SUMMARY.md` when done
</output>
