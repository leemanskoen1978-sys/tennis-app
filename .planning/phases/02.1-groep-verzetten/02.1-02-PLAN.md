---
phase: 02.1-groep-verzetten
plan: 02
type: execute
wave: 2
depends_on: [02.1-01]
files_modified:
  - providers/SimpleDataProvider.tsx
  - app/admin/lesgroepen/[id].tsx
  - lib/i18n-en.ts
autonomous: false
requirements: [GROEP-05]

must_haves:
  truths:
    - "De beheerder verzet een groep naar een ander uur, een andere dag, een andere trainer of een andere baan en ziet de komende lessen mee verzet"
    - "Vóór het bewaren staat op het scherm hoeveel lessen er mee verzetten en welke er botsen, met dag en reden erbij"
    - "Een botsende les blijft staan waar hij stond en wordt niet stil overschreven"
    - "De groepsrij en al haar boekingen gaan in één opslag weg — nooit half"
  artifacts:
    - path: "providers/SimpleDataProvider.tsx"
      provides: "updateLesGroep dat via planGroepWijziging groep en lessen samen wegschrijft"
      contains: "planGroepWijziging"
    - path: "app/admin/lesgroepen/[id].tsx"
      provides: "de voorvertoning van de gevolgen bij de Bewaren-knop"
      contains: "planGroepWijziging"
    - path: "lib/i18n-en.ts"
      provides: "de Engelse tegenhanger van elke nieuwe zin"
      contains: "lesson"
  key_links:
    - from: "providers/SimpleDataProvider.tsx"
      to: "lib/lesgroepen.ts"
      via: "planGroepWijziging, één commit met groep + bookingPatches"
      pattern: "planGroepWijziging\\("
    - from: "app/admin/lesgroepen/[id].tsx"
      to: "lib/lesgroepen.ts"
      via: "planGroepWijziging voor de voorvertoning; het scherm rekent zelf niets uit"
      pattern: "planGroepWijziging\\("
---

<objective>
De schrijfweg en de melding: `updateLesGroep` verzet voortaan de komende lessen mee via
`planGroepWijziging`, in één opslag met de groepsrij, en het detailscherm toont vóór het bewaren
wat er gaat gebeuren en wat er botst.

Purpose: het plan uit 02.1-01 rekent, maar niets schrijft het weg en niemand ziet het. Dit plan
sluit GROEP-05: uur, dag, trainer en baan werken door, en de botsingen worden gemeld in plaats van
stil overschreven (D-06).

Output: een `updateLesGroep` die groep en lessen samen wegschrijft en het plan teruggeeft, een
voorvertoning op `app/admin/lesgroepen/[id].tsx` in de vorm die `components/BookingModal.tsx` al
gebruikt, en elke nieuwe zin met zijn Engelse tegenhanger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-groep-verzetten/02.1-CONTEXT.md
@.planning/phases/01-lesgroepen/01-05-SUMMARY.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Wat plan 02.1-01 oplevert; hierop bouwt dit plan. -->

lib/lesgroepen.ts (na 02.1-01):
  export interface VerzetPatch { id: string; start_time: string; end_time: string; coach_id: string; court_id: string }
  export interface GeblokkeerdeLes { id: string; start_time: string; reden: 'bezet' | 'vakantie' | 'verleden'; vakantie?: string; conflict?: string }
  export interface GroepWijzigingPlan { group: LesGroep; bookingPatches: VerzetPatch[]; geblokkeerd: GeblokkeerdeLes[] }
  export function planGroepWijziging(group: LesGroep, patch: Partial<Omit<LesGroep, 'id' | 'roster'>>, bookings: VerzetBoeking[], now: Date, vakanties?: Vakantie[]): GroepWijzigingPlan

providers/SimpleDataProvider.tsx (bestaand):
  updateLesGroep: (id: string, patch: Partial<Omit<LesGroep, 'id' | 'roster'>>) => Promise<void>   // regel ~198 (type) en ~1079 (implementatie)
  updateLesGroepRoster — het te volgen voorbeeld op regel ~1091: één plan uit lib/, één commit met lesGroepen én bookings samen
  store.settings.vakanties ?? []  — zoals op regel 648 al gebeurt voor addBookingSeries

app/admin/lesgroepen/[id].tsx (bestaand):
  interface Concept { naam; niveau; weekdag; beginuur /* 'HH:MM' */; trainerId; baanId; van; tot }
  const bewaar = (): void => { ... lesGroepFout ... void updateLesGroep(groep.id, patch); setConcept(null); }   // regel ~140
  const now = useMemo(() => new Date(), []);   // één moment voor het hele scherm
  useSimpleData() levert hier al: currentUser, users, courts, bookings, lesGroepen, updateLesGroep, updateLesGroepRoster, archiveLesGroep, error — `settings` moet erbij

components/BookingModal.tsx (het te volgen voorbeeld voor de melding):
  regels 69-82   overgeslagenZin(skipped) — twee redenen, apart geteld
  regels 197-204 const plan = planSeries(...); const overgeslagen = (reden) => plan?.skipped.filter(...)
  regels 566-600 de voorvertoning in beeld: één regel per reden, met de dagen erbij via formatDay

lib/datetime.ts: export function formatDay(iso: Moment): string
lib/i18n.ts: useT() in een scherm, t() daarbuiten; elke Nederlandse zin krijgt een sleutel in lib/i18n-en.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Taak 1: updateLesGroep schrijft de groep en haar komende lessen samen weg</name>
  <files>providers/SimpleDataProvider.tsx</files>

  <read_first>
    - providers/SimpleDataProvider.tsx regels 186-215 (de doc-commentaren bij `updateLesGroep`, `updateLesGroepRoster` en `archiveLesGroep`)
    - providers/SimpleDataProvider.tsx regels 1060-1130 (`addLesGroep`, `updateLesGroep`, `updateLesGroepRoster`, `archiveLesGroep`)
    - providers/SimpleDataProvider.tsx regels 640-660 (hoe `store.settings.vakanties ?? []` daar al meegaat)
    - lib/lesgroepen.ts na 02.1-01 (`planGroepWijziging` en zijn typen)
  </read_first>

  <action>
    Laat `updateLesGroep` niet langer alleen de groepsrij patchen. Volg woordelijk de vorm van
    `updateLesGroepRoster` ernaast: het plan komt uit `lib/lesgroepen`, de provider schrijft het
    weg — er wordt hier niets bedacht over welke lessen het aangaat.

    In de implementatie: de groep opzoeken (niet gevonden → stilletjes terug, zoals
    `updateLesGroepRoster`), dan `planGroepWijziging(groep, patch, store.bookings, new Date(),
    store.settings.vakanties ?? [])`. Maak van `plan.bookingPatches` een `Map` op id, precies zoals
    `updateLesGroepRoster` dat met `gepatcht` doet, en commit in één keer: `lesGroepen` met
    `plan.group` op zijn plaats, en `bookings` waarin elke geraakte boeking `start_time`,
    `end_time`, `coach_id` en `court_id` uit haar patch krijgt. Spreid de patch met
    `{ ...b, ...p }` en niet veld voor veld, zodat er nooit een veld half meegaat — en juist
    daarom bevat `VerzetPatch` geen `taught_by_id`.

    Verander het retourtype naar `Promise<GroepWijzigingPlan | null>` (`null` als de groep niet
    bestaat), in beide plaatsen: het `DataShape`-type bovenaan én de `useCallback`. Zo kan het
    scherm melden wat er werkelijk gebeurd is, net zoals `addBookingSeries` `{ created, skipped }`
    teruggeeft en `addLesGroep` de gemaakte groep.

    Werk het doc-commentaar bij `updateLesGroep` in `DataShape` bij: dat het niet langer één rij
    is, dat uur, dag, trainer en baan doorwerken in de lessen van vandaag en later, dat wat geweest
    is blijft staan, dat `taught_by_id` nooit meegaat, en dat botsende lessen in `geblokkeerd`
    terugkomen in plaats van overschreven te worden. Zet er bij de commit een Nederlands commentaar
    bij met dezelfde reden als bij het rooster: half doorgevoerd zou een groep opleveren die niet
    klopt met haar eigen lessen — het scherm zegt dan 18u terwijl de lessen nog op 17u staan.

    Raak `updateLesGroepRoster`, `archiveLesGroep` en `addLesGroep` niet aan. Geen SQL, geen
    schemawijziging, geen nieuwe kolom (D-09, D-10).
  </action>

  <verify>
    <automated>npx tsc --noEmit && npm test -- --silent</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "planGroepWijziging" providers/SimpleDataProvider.tsx` → minstens 2 (import + aanroep)
    - `grep -c "GroepWijzigingPlan | null" providers/SimpleDataProvider.tsx` → 2 (het type in `DataShape` en de `useCallback`)
    - `grep -n "updateLesGroep = useCallback" -A 25 providers/SimpleDataProvider.tsx | grep -c "await commit("` → 1 (één opslag, niet twee)
    - `grep -n "updateLesGroep = useCallback" -A 25 providers/SimpleDataProvider.tsx | grep -c "taught_by_id"` → 0
    - `grep -c "vakanties" providers/SimpleDataProvider.tsx` → minstens 2 (de bestaande reeksplek plus deze)
    - `git diff providers/SimpleDataProvider.tsx | grep -c "updateLesGroepRoster = useCallback"` → 0 (de roosterweg is niet aangeraakt)
    - `git diff --stat supabase-schema.sql` → leeg
    - `npx tsc --noEmit` → schoon; `npm test` → alle bestaande suites groen
  </acceptance_criteria>

  <done>
    Een wijziging aan dag, uur, trainer of baan van een groep zet de groepsrij én de komende
    boekingen in één opslag, en de aanroeper krijgt het plan met de geblokkeerde lessen terug.
  </done>
</task>

<task type="auto">
  <name>Taak 2: het scherm meldt wat er gaat gebeuren, vóór het bewaren</name>
  <files>app/admin/lesgroepen/[id].tsx, lib/i18n-en.ts</files>

  <read_first>
    - app/admin/lesgroepen/[id].tsx (volledig — het kopcommentaar over "dit scherm rekent zelf niets uit", `Concept`, `conceptVan`, `bewaar`, de kaart met het formulier en de `Bewaren`-knop, de styles onderaan)
    - components/BookingModal.tsx regels 60-85 en 190-210 en 560-605 (de voorvertoning-vorm die hier herhaald wordt)
    - lib/i18n-en.ts regels 1140-1174 (het lesgroepen-blok waar de nieuwe zinnen bij horen)
    - lib/datetime.ts (`formatDay`, `formatTime`)
    - lib/lesgroepen.ts na 02.1-01 (`planGroepWijziging`, `GeblokkeerdeLes`)
  </read_first>

  <action>
    Haal `settings` er in `useSimpleData()` bij en bouw de patch één keer op een plek: trek uit
    `bewaar` het samenstellen van `patch` (naam, niveau, weekdag, uur, minuut, `coach_id`,
    `court_id`, seizoen) in een lokale functie `patchVan(huidig)`, zodat de voorvertoning en het
    bewaren gegarandeerd dezelfde wijziging bekijken. Twee plekken die elk hun eigen patch bouwen
    zouden een botsing kunnen tonen die bij het bewaren niet meer bestaat.

    Zet daarboven een `useMemo` die de voorvertoning berekent:
    `concept === null ? null : planGroepWijziging(groep, patchVan(huidig), bookings, now,
    settings.vakanties ?? [])`. Niets aangeraakt betekent niets te melden. `now` is de bestaande
    `useMemo`-datum van dit scherm; maak er geen tweede.

    Toon de uitkomst tussen de foutregel en de `Bewaren`-knop, in dezelfde toon als de reeksmelding
    van `BookingModal` — eerst wat er gebeurt, dan per reden wat er niet gebeurt, elk met de dagen
    erbij. `t()` met tellingen als variabele, geen zin in elkaar geplakt uit losse stukken:
    - Verzetten er lessen mee: *"{lessen} van vandaag en later verzetten mee. De lessen die al
      geweest zijn blijven staan waar ze stonden."*
    - `reden: 'bezet'`: *"{lessen} blijven staan: de trainer of de baan is dan al bezet: {dagen}."*
    - `reden: 'vakantie'`: *"{lessen} zouden in een vakantie vallen en blijven staan: {dagen}."*
    - `reden: 'verleden'`: *"{lessen} zouden hierdoor in het verleden komen te staan en blijven
      staan: {dagen}."*
    - Verandert er niets aan dag, uur, trainer of baan (`bookingPatches` en `geblokkeerd` allebei
      leeg): *"Naam, niveau en seizoen raken de lessen niet; er verzet niets mee."*
    Gebruik `formatDay` uit lib/datetime voor elke dag; geen eigen datumopmaak. Hergebruik de
    bestaande `styles.uitleg`/`styles.fout`-vormen van dit bestand in plaats van nieuwe kleuren te
    verzinnen.

    Zet er één zin boven de knop die de regel eronder uitlegt, in dezelfde geest als de zin die er
    bij de spelers al staat: *"Een ander uur, een andere dag, een andere trainer of een andere
    baan werkt door in de lessen van vandaag en later. Een les die iemand anders al gaf, blijft
    van hem."* — dat laatste omdat `taught_by_id` niet meebeweegt (D-04, D-05) en de beheerder dat
    hoort te weten zonder de code te lezen.

    `bewaar` blijft valideren met `lesGroepFout` en roept daarna `updateLesGroep(groep.id,
    patchVan(huidig))` aan. Er komt geen tweede regelset in dit scherm: geen eigen lus over
    boekingen, geen eigen overlapvergelijking, geen eigen datumrekenwerk. Het kopcommentaar van
    het bestand krijgt er één alinea bij die dat vastlegt voor wie hierna komt.

    Zet elke nieuwe Nederlandse zin met zijn Engelse tegenhanger in `lib/i18n-en.ts`, in het
    bestaande lesgroepen-blok, met een `//`-regel erboven die zegt waar ze staan.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npm test -- --silent && npx expo export --platform web</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "planGroepWijziging" 'app/admin/lesgroepen/[id].tsx'` → minstens 2 (import + de ene aanroep in de useMemo)
    - `grep -c "patchVan" 'app/admin/lesgroepen/[id].tsx'` → 3 (definitie + de voorvertoning + `bewaar`)
    - `grep -c "for (\|while (\|setDate(\|aStart < bEnd" 'app/admin/lesgroepen/[id].tsx'` → 0 (geen eigen rekenwerk)
    - `grep -c "taught_by_id" 'app/admin/lesgroepen/[id].tsx'` → 0
    - `grep -c "settings" 'app/admin/lesgroepen/[id].tsx'` → minstens 2 (uit `useSimpleData` + `settings.vakanties`)
    - `grep -c "new Date()" 'app/admin/lesgroepen/[id].tsx'` → 1 (de bestaande `now`-useMemo, geen tweede moment)
    - Elke `t('...')`-sleutel die dit plan toevoegt bestaat in `lib/i18n-en.ts` — loop het na met een scriptje dat de sleutels uit het scherm haalt en tegen `lib/i18n-en.ts` legt; uitkomst: niets ontbreekt
    - `npx expo export --platform web` → `Exported: dist`, exit 0
  </acceptance_criteria>

  <done>
    De beheerder ziet vóór het bewaren hoeveel lessen mee verzetten en welke er om welke reden
    blijven staan, en het scherm rekent daarvoor niets zelf uit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Taak 3: met de hand nalopen dat het verzetten klopt</name>
  <files>geen — deze taak wijzigt geen enkel bestand</files>
  <action>Draai de app en loop de zes stappen onder &lt;how-to-verify&gt; na. Wijzig niets; noteer per stap wat je zag. Wijkt een stap af, dan is dat een bevinding voor de samenvatting en niet iets om ter plekke recht te zetten.</action>

  <what-built>
    `updateLesGroep` verzet de komende lessen van een groep mee (dag, uur, trainer, baan) in één
    opslag met de groepsrij, en het groepsdetailscherm toont vóór het bewaren wat er gaat gebeuren
    en wat er blijft staan. Wat geweest is verandert niet, en `taught_by_id` blijft staan.
  </what-built>

  <how-to-verify>
    1. Start de app en ga naar Beheer → Lesgroepen → een groep met lessen.
    2. Kies een ander beginuur. Er hoort onder het formulier te verschijnen hoeveel lessen mee
       verzetten, nog vóór je op Bewaren drukt.
    3. Druk op Bewaren. Kijk in de kaart "Lessen": de komende lessen staan op het nieuwe uur, en
       het blok "Eerder en afgezegd" is onveranderd — dezelfde datums en dezelfde uren als daarvoor.
    4. Zet de groep op een dag/uur waarop diezelfde trainer al een andere les heeft staan. De
       melding hoort te zeggen dat die les blijft staan omdat de trainer of de baan dan bezet is,
       met de dag erbij — en na Bewaren staat die ene les nog op zijn oude moment.
    5. Zet de groep op een andere trainer. De komende lessen staan bij de nieuwe trainer; een les
       waar eerder een vervanger op stond toont die vervanger nog steeds.
    6. Wijzig alleen de naam en bewaar: er hoort te staan dat er niets meeverzet, en geen enkele
       les mag van plaats veranderen.
  </how-to-verify>

  <verify>
    <human-check>De zes stappen hierboven gedaan, met de uitkomst per stap.</human-check>
  </verify>

  <resume-signal>Typ "goedgekeurd", of beschrijf per stap wat er niet klopte</resume-signal>

  <done>
    De beheerder heeft met eigen ogen gezien dat verzetten doorwerkt, dat het verleden onaangeroerd
    blijft, dat botsingen gemeld worden in plaats van overschreven, en dat een vervanger blijft staan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scherm → provider | De beheerder stuurt een groepspatch die tientallen boekingen herschrijft |
| provider → opslag | Groep en boekingen moeten samen weg, of geen van beide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-06 | Tampering | `updateLesGroep` schrijft de groep weg maar de lessen niet (of omgekeerd) | mitigate | Eén `await commit(...)` met `lesGroepen` én `bookings` samen; grep-criterium telt er precies één |
| T-02.1-07 | Tampering | De boekingspatch neemt meer velden mee dan bedoeld en wist bv. `taught_by_id` | mitigate | `{ ...b, ...p }` met `VerzetPatch` zonder dat veld; grep op `taught_by_id` in het blok → 0 |
| T-02.1-08 | Elevation of privilege | Een trainer tikt de detail-URL in en verzet een groep | mitigate | De bestaande `isAdmin(currentUser)`-grens bovenaan het scherm blijft ongewijzigd vóór elke andere return; RLS blijft de echte bewaking (fase 1 plan 07) |
| T-02.1-09 | Repudiation | Een botsing wordt getoond maar bij het bewaren toch overschreven | mitigate | Voorvertoning en schrijfweg gaan door dezelfde `planGroepWijziging` met dezelfde `patchVan`; de checkpoint loopt het geval met de hand na |
| T-02.1-10 | Denial of service | — | accept | Enkele tientallen boekingen per groep; dezelfde commit-vorm als `updateLesGroepRoster` die al draait |
| T-02.1-SC | Tampering | npm/pip/cargo installs | accept | Dit plan installeert geen enkel pakket |
</threat_model>

<verification>
- `npx tsc --noEmit` → schoon
- `npm test` → alle suites groen, geen bestaande test aangepast om groen te worden
- `npx expo export --platform web` → geslaagd
- `git diff --stat supabase-schema.sql lib/types.ts` → leeg (geen kolom, geen tabel, geen migratie)
- Geen SQL gedraaid en geen verbinding met Supabase gemaakt
- De handmatige controle uit taak 3 is doorlopen en goedgekeurd
</verification>

<success_criteria>
- Uur, dag, trainer en baan van een groep verzetten werkt door in alle lessen van vandaag en later
- Lessen die al geweest zijn staan onveranderd op hun eigen moment, met hun eigen trainer en
  deelnemers
- Een komende les met een vervanger houdt die vervanger terwijl haar `coach_id` meeverhuist
- Botsende en in-een-vakantie-vallende lessen worden gemeld vóór het bewaren en blijven staan
- Het scherm rekent niets uit: elk antwoord komt uit `lib/lesgroepen`
- GROEP-05 kan op Complete
</success_criteria>

<output>
Create `.planning/phases/02.1-groep-verzetten/02.1-02-SUMMARY.md` when done
</output>
