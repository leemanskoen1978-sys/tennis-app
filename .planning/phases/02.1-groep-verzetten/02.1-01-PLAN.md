---
phase: 02.1-groep-verzetten
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/recurrence.ts
  - lib/recurrence.test.ts
  - lib/lesgroepen.ts
  - lib/lesgroepen.test.ts
autonomous: true
requirements: [GROEP-05]

must_haves:
  truths:
    - "Een groep naar een andere dag of een ander uur verzetten levert een plan op waarin elke komende les mee verzet is en geen enkele les van vroeger voorkomt"
    - "Een groep aan een andere trainer geven levert per komende les een nieuwe coach_id op, terwijl taught_by_id in geen enkele patch voorkomt"
    - "Een komende les die na het verzetten op een bezette trainer of een bezette baan zou uitkomen, staat in de gemelde lijst en niet in de patches"
    - "Een komende les die na het verzetten in een clubvakantie zou vallen, staat in de gemelde lijst met de naam van de vakantie erbij"
    - "Een groep die over de uurwissel heen verzet wordt, houdt op elke lesdatum hetzelfde lokale uur"
    - "Een wijziging aan enkel naam, niveau of seizoen levert nul boekingspatches op"
  artifacts:
    - path: "lib/recurrence.ts"
      provides: "botstMet als de ene, nu geexporteerde botsingsregel voor trainer en baan"
      contains: "export function botstMet"
    - path: "lib/lesgroepen.ts"
      provides: "planGroepWijziging: het plan voor het verzetten van een groep"
      contains: "export function planGroepWijziging"
    - path: "lib/lesgroepen.test.ts"
      provides: "de bewijzen: verleden ongemoeid, taught_by_id ongemoeid, botsingen gemeld, uurwissel"
      contains: "planGroepWijziging"
  key_links:
    - from: "lib/lesgroepen.ts"
      to: "lib/recurrence.ts"
      via: "import van botstMet — geen tweede versie van de botsingsregel"
      pattern: "botstMet.*from './recurrence'"
    - from: "lib/lesgroepen.ts"
      to: "lib/vakanties.ts"
      via: "vakantieOpMoment, dezelfde vakantieregel als planSeries"
      pattern: "vakantieOpMoment"
---

<objective>
Het rekenwerk achter "de groep verzetten werkt door": één pure functie `planGroepWijziging` in
`lib/lesgroepen.ts`, naast `planRosterChange` en in dezelfde vorm — een plan eruit, geen opslag
erin. Ze zegt welke komende lessen mee verzet worden, welke botsen of in een vakantie vallen, en
raakt `taught_by_id` nooit.

Purpose: fase 1 leverde de helft van GROEP-05 (spelers erbij of eraf). De andere helft — ander
uur, andere dag, andere trainer, andere baan — verzet vandaag geen enkele ingeplande les
(01-05-SUMMARY). Die helft raakt de botsingscontrole én de betekenis van `coach_id` die fase 2
net apart zet van `taught_by_id`, en hoort daarom in `lib/` te staan met een test eromheen.

Output: `botstMet` geëxporteerd uit `lib/recurrence.ts` (trainer én baan, één implementatie),
`planGroepWijziging` + zijn typen in `lib/lesgroepen.ts`, en de bewijzen in beide testbestanden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-groep-verzetten/02.1-CONTEXT.md
@.planning/phases/01-lesgroepen/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Wat er vandaag staat. Hier bouwt dit plan op verder; niets hiervan hoeft opgezocht te worden. -->

lib/recurrence.ts (bestaand, regels 44-52 en 98-115):
  export interface SeriesSlot { start_time: string; end_time: string }
  export type OvergeslagenReden = 'bezet' | 'vakantie'
  export interface OvergeslagenSlot extends SeriesSlot { reden: OvergeslagenReden; vakantie?: string }
  export interface SeriesPlan { usable: SeriesSlot[]; skipped: OvergeslagenSlot[] }
  function collides(slot: SeriesSlot, coachId: string, existing: Booking[]): boolean   // PRIVAAT vandaag
  export function planSeries(startTime, endTime, rule, coachId, existing: Booking[], vakanties: Vakantie[] = []): SeriesPlan
  function shiftDays(d: Date, days: number): Date   // lokale dag-optelling, DST-veilig

lib/lesgroepen.ts (bestaand):
  export type GroepBoeking = Pick<Booking, 'id' | 'group_id' | 'start_time' | 'status' | 'participant_ids'>
  export interface RosterChangePlan { group: LesGroep; bookingPatches: Array<{ id: string; participant_ids: string[] }> }
  export function komendeLessen<B extends GroepBoeking>(bookings: B[], groupId: string, now: Date): B[]
  export function planRosterChange(group, newRoster, bookings, now): RosterChangePlan

lib/vakanties.ts:
  export function vakantieOpMoment(vakanties: Vakantie[], iso: string): Vakantie | null

lib/types.ts:
  Booking: { id, player_id, participant_ids?, coach_id, taught_by_id?, court_id, start_time, end_time, status, payment_method, series_id?, group_id?, ... }
  LesGroep: { id, name, level, weekday /* 0-6, zondag = 0 */, start_hour, start_minute, coach_id?, court_id?, season_start, season_end, roster, archived }

providers/SimpleDataProvider.tsx regel 251 (niet aanraken in dit plan, wel de reden dat botstMet
één implementatie moet blijven):
  function overlaps(a, list): boolean   // woordelijk dezelfde overlapregel voor de trainer
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: Eén botsingsregel, nu ook voor de baan</name>
  <files>lib/recurrence.ts, lib/recurrence.test.ts</files>

  <read_first>
    - lib/recurrence.ts (volledig — vooral het kopcommentaar over lokale tijd, `collides` op regel ~98 met zijn commentaar over `overlaps`, en `planSeries`)
    - lib/recurrence.test.ts (volledig — `process.env.TZ` bovenaan, de helpers `iso`, `booking`, `busy`, en het blok 'de reeks opbouwen')
    - providers/SimpleDataProvider.tsx regels 245-260 (`overlaps`, de zin die `collides` woordelijk zegt te volgen)
    - lib/types.ts, de `Booking`-interface (regels 155-215)
  </read_first>

  <behavior>
    Nieuw geëxporteerd uit `lib/recurrence.ts`, met de tests eerst in `lib/recurrence.test.ts`:
    - Een tijdvak dat de bestaande les van dezelfde trainer overlapt levert die botsende boeking op.
    - Een tijdvak dat aansluit maar niet overlapt (10-11 tegen 11-12) levert `null`.
    - Een afgezegde boeking houdt niets bezet: `null`.
    - Een boeking van een andere trainer op dezelfde baan, met `courtId` meegegeven, levert die
      boeking op — de baan is dan bezet ook al is de trainer vrij.
    - Zonder `courtId` in de vraag wordt de baan niet gekeken (het bestaande gedrag van `planSeries`).
    - Een boeking waarvan de id in `negeer` staat telt niet mee als botsing, ook niet bij volledige
      overlap — dat is de weg waarlangs een groep niet met haar eigen lessen botst.
    - `planSeries` blijft zich exact gedragen zoals de bestaande tests eisen: alle 285 regels aan
      bestaande verwachtingen in `lib/recurrence.test.ts` blijven ongewijzigd en groen.
  </behavior>

  <action>
    Maak van de private `collides` in `lib/recurrence.ts` de geëxporteerde, gedeelde regel. Geen
    tweede implementatie erbij: `planSeries` gaat er zelf ook doorheen (D-07).

    Exporteer een smalle boekingsvorm `BezetBoeking` als `Pick<Booking, 'id' | 'coach_id' |
    'court_id' | 'start_time' | 'end_time' | 'status'>`, in dezelfde geest als `GroepBoeking` in
    lib/lesgroepen: dit bestand hoeft niet meer van een les te weten om de vraag te beantwoorden.

    Exporteer `BezetVraag` met `coachId: string`, `courtId?: string` en
    `negeer?: ReadonlySet<string>`, en `botstMet(slot: SeriesSlot, existing: BezetBoeking[],
    vraag: BezetVraag): BezetBoeking | null` — de eerste botsende boeking, of `null`. Geef de
    boeking zelf terug en niet enkel `true`, zodat de melder later kan zeggen wáármee het botst;
    `planSeries` leest de uitkomst als `!== null` en verandert verder geen letter.

    De regel binnenin blijft woordelijk wat `collides` deed — een afgezegde boeking houdt niets
    bezet, grenzen tellen niet mee (10-11 botst niet met 11-12) — met er twee dingen bij:
    een boeking waarvan de id in `negeer` staat wordt overgeslagen, en een boeking botst ook als
    `vraag.courtId` gezet is en gelijk aan haar `court_id`. Sla een boeking met een lege
    `court_id` over bij de baanvraag: een les zonder baan houdt geen baan bezet.

    Neem het bestaande commentaar van `collides` mee naar boven en breid het uit met WAAROM de
    baan erbij hoort en waarom er precies één versie van deze vraag bestaat: wijkt deze regel af
    van `overlaps` in providers/SimpleDataProvider, dan meldt het scherm een verzetting die de
    provider vervolgens weigert. Benoem `negeer` als "de lessen die zelf aan het verhuizen zijn",
    met de reden: zonder die uitzondering botst elke groep met haar eigen oude uur.

    Voeg de tests uit `<behavior>` toe als een eigen `describe('botstMet')` in
    `lib/recurrence.test.ts`, met de bestaande helpers `iso`, `booking` en `busy`. Verwijder of
    verander geen enkele bestaande verwachting in dat bestand.
  </action>

  <verify>
    <automated>npx jest lib/recurrence --silent && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "export function botstMet" lib/recurrence.ts` → 1
    - `grep -c "^function collides" lib/recurrence.ts` → 0 (er is geen tweede implementatie meer)
    - `grep -c "botstMet(" lib/recurrence.ts` → minstens 2 (de definitie plus de aanroep in `planSeries`)
    - `grep -c "describe('botstMet'" lib/recurrence.test.ts` → 1, met minstens 6 `it(`-gevallen erin
    - `git diff lib/recurrence.test.ts | grep '^-' | grep -v '^---' | grep -c "expect"` → 0 (geen bestaande verwachting geschrapt)
    - `npx jest lib/recurrence --silent` → alle suites groen
    - `grep -v "^//" lib/recurrence.ts | grep -c "from '\.\./"` → 0 (lib importeert nergens uit providers/components/app)
  </acceptance_criteria>

  <done>
    `botstMet` is de enige plek in `lib/` die "is deze trainer of deze baan dan bezet" beantwoordt,
    `planSeries` loopt erdoorheen zonder gedragsverandering, en de baan- en `negeer`-gevallen zijn
    getest.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Taak 2: planGroepWijziging — wat het verzetten met de komende lessen doet</name>
  <files>lib/lesgroepen.ts, lib/lesgroepen.test.ts</files>

  <read_first>
    - lib/lesgroepen.ts (volledig — het kopcommentaar over de grens groep/boeking, `komendeLessen`, `planRosterChange`, `RosterChangePlan`)
    - lib/lesgroepen.test.ts (volledig — de helpers `base`, `les`, `groep`, `nu`, en het blok rond `planRosterChange`)
    - lib/recurrence.ts na taak 1 (`botstMet`, `BezetVraag`, `shiftDays` en het kopcommentaar over lokale tijd)
    - lib/recurrence.test.ts regels 1-10 en 57-69 (de `process.env.TZ`-regel en de bestaande uurwisseltest — het sjabloon voor de fixture hier)
    - lib/vakanties.ts (`vakantieOpMoment`)
    - .planning/phases/02-wie-gaf-de-les-echt/02-01-SUMMARY.md (wat `taught_by_id` betekent en waarom het onaangeroerd blijft)
  </read_first>

  <behavior>
    Tests eerst in `lib/lesgroepen.test.ts`, in een eigen `describe('planGroepWijziging')`. Zet
    bovenaan het bestand `process.env.TZ = 'Europe/Brussels'` vóór elke import, met dezelfde
    verantwoording als in `lib/recurrence.test.ts`, en gebruik voor de nieuwe gevallen lokale
    datums (een `iso(y, m, d, h)`-helper zoals daar) in plaats van de bestaande Z-strings.

    - Een les van vóór `now` komt in geen enkele patch voor en het meegegeven object is na afloop
      byte voor byte hetzelfde (JSON-vergelijking, zoals de bestaande mutatietest).
    - Een komende les krijgt een `start_time` op de nieuwe weekdag en het nieuwe uur, en een
      `end_time` die dezelfde lesduur houdt als die les zelf had.
    - Een andere trainer: elke komende les krijgt `coach_id` van de nieuwe trainer.
    - Een komende les met een ingevulde `taught_by_id` krijgt óók de nieuwe `coach_id`, en
      `taught_by_id` komt in geen enkele patch voor (`'taught_by_id' in patch` is `false`) en is op
      de meegegeven boeking onveranderd (D-04, D-05).
    - Een komende les die na het verzetten op een bezette trainer uitkomt, staat in `geblokkeerd`
      met `reden: 'bezet'` en heeft géén patch.
    - Idem voor een bezette baan wanneer de groep een `court_id` heeft.
    - De andere lessen van dezelfde groep tellen niet als botsing: een groep die één uur opschuift
      botst niet met haar eigen volgende week.
    - Een komende les die na het verzetten in een clubvakantie valt, staat in `geblokkeerd` met
      `reden: 'vakantie'` en de naam van de vakantie, en heeft géén patch — de vakantie gaat vóór
      de bezetvraag, net als in `planSeries` (D-08).
    - Een verzetting die een les naar een moment vóór `now` zou brengen levert `reden: 'verleden'`
      op en geen patch: er verhuist nooit iets het verleden in (D-01).
    - Een afgezegde komende les krijgt geen patch en geen melding.
    - Fixture over de uurwissel: een groep met lessen in oktober en november wordt naar een ander
      uur verzet; elke patch heeft op zijn nieuwe datum exact hetzelfde lokale uur en dezelfde
      lokale minuut (geen uur verschoven door DST).
    - Een wijziging van enkel `name`, `level`, `season_start` of `season_end` levert nul patches en
      nul meldingen op, en `plan.group` draagt de nieuwe waarden (D-03).
    - `plan.group` is een nieuw object; de meegegeven groep is ongemoeid.
  </behavior>

  <action>
    Schrijf `planGroepWijziging` in `lib/lesgroepen.ts`, direct onder `planRosterChange`, met een
    kopcommentaar in de stijl van dat bestand: WAAROM ze bestaat (fase 1 verzette de groepsrij en
    geen enkele les), en welke grens ze bewaakt.

    Typen ernaast, geëxporteerd:
    - `VerzetBoeking` als `Pick<Booking, 'id' | 'group_id' | 'coach_id' | 'taught_by_id' |
      'court_id' | 'start_time' | 'end_time' | 'status'>` — de velden die deze vraag nodig heeft,
      meer niet, zoals `GroepBoeking` hierboven.
    - `VerzetPatch` met `id`, `start_time`, `end_time`, `coach_id`, `court_id`. `taught_by_id`
      staat er met opzet NIET in; zet dat als commentaar bij het type, met de reden: een les die
      iemand anders al gaf, blijft van hem, en zijn loon ook (D-04).
    - `GeblokkeerdeLes` met `id`, `start_time` (het nieuwe, voorgestelde moment), `reden:
      'bezet' | 'vakantie' | 'verleden'`, `vakantie?: string` en `conflict?: string` (de id van de
      boeking waarmee het botst).
    - `GroepWijzigingPlan` met `group: LesGroep`, `bookingPatches: VerzetPatch[]` en
      `geblokkeerd: GeblokkeerdeLes[]`.

    Signatuur: `planGroepWijziging(group: LesGroep, patch: Partial<Omit<LesGroep, 'id' |
    'roster'>>, bookings: VerzetBoeking[], now: Date, vakanties: Vakantie[] = []):
    GroepWijzigingPlan`. Dezelfde vorm als `planRosterChange`: een plan eruit, geen opslag erin,
    niets gemuteerd. Dezelfde `patch`-vorm als `updateLesGroep` in de provider, zodat het scherm
    één object heeft en niet twee.

    De regels binnenin:
    1. `nieuw = { ...group, ...patch }` is `plan.group`.
    2. Verschillen alleen `weekday`, `start_hour`, `start_minute`, `coach_id` of `court_id` van
       de oude groep? Zo niet, dan is het plan leeg op de groep na — naam, niveau en seizoen raken
       geen enkele les (D-03).
    3. De lessen die het aangaat zijn `komendeLessen(bookings, group.id, now)`. Geen eigen filter
       erbij: dezelfde "vanaf vandaag"-grens als `planRosterChange` en `seriesFrom`, één keer
       vastgelegd (D-01).
    4. Het nieuwe moment per les, uitsluitend met dag-, uur- en minuutvelden — nooit
       milliseconden optellen. Bereken de dagverschuiving als het verschil tussen de oude en de
       nieuwe weekdag in maandag-eerste telling (`(d + 6) % 7`, dezelfde leesvolgorde als
       `DAG_VOLGORDE` op het scherm), zodat een les binnen zijn eigen week verhuist en er nooit
       een week bij of af komt. Bouw het nieuwe begin als `new Date(jaar, maand, dag + verschil,
       nieuw.start_hour, nieuw.start_minute, 0, 0)` en het nieuwe einde met dezelfde constructie
       plus de duur van die les in minuten (`(end - start) / 60000`, per les afzonderlijk — de
       clubinstelling `lesson_duration_minutes` telt uitdrukkelijk niet met terugwerkende kracht,
       zie het commentaar bij dat veld in lib/types). Zet een commentaar bij deze berekening dat
       de reden noemt: bij "168 uur erbij" staat de les van na de uurwissel ineens om 09:00 of
       11:00 (D-05 van fase 1 / de kop van lib/recurrence).
    5. Ligt het nieuwe begin vóór `now`, dan `reden: 'verleden'` en geen patch.
    6. Anders eerst de vakantie via `vakantieOpMoment(vakanties, nieuwBegin)` — is de club dicht,
       dan doet het er niet meer toe of de trainer dan ook nog bezet was, exact de volgorde van
       `planSeries` (D-08).
    7. Anders `botstMet` uit lib/recurrence met `{ coachId: nieuw.coach_id ?? les.coach_id,
       courtId: nieuw.court_id, negeer }`, waarbij `negeer` de ids van álle komende lessen van
       deze groep bevat — een groep botst niet met zichzelf. Botst het, dan `reden: 'bezet'` met
       de id van de gevonden boeking in `conflict`, en geen patch. Schrijf hier geen eigen
       overlapvergelijking: er is één versie van die vraag (D-07).
    8. Anders een `VerzetPatch` met het nieuwe begin en einde als ISO, `coach_id` als
       `nieuw.coach_id ?? les.coach_id` en `court_id` als `nieuw.court_id ?? les.court_id` — een
       groep zonder baan haalt de baan van een les niet weg, want een boeking heeft er een nodig.

    Importeer `botstMet` uit `./recurrence` en `vakantieOpMoment` uit `./vakanties`; geen import
    uit `providers/`, `components/` of `app/`. Geen `jest.mock`/`jest.fn` in de test.
  </action>

  <verify>
    <automated>npx jest lib/lesgroepen --silent && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "export function planGroepWijziging" lib/lesgroepen.ts` → 1
    - `grep -c "botstMet" lib/lesgroepen.ts` → minstens 2 (import + aanroep); `grep -c "aStart < bEnd" lib/lesgroepen.ts` → 0 (geen eigen overlapregel)
    - `grep -c "vakantieOpMoment" lib/lesgroepen.ts` → minstens 2
    - `grep -n "taught_by_id" lib/lesgroepen.ts` → uitsluitend commentaarregels en de `Pick<>`-opsomming van `VerzetBoeking`; nergens in een patch-literal
    - `grep -c "getTime() + \|60 \* 60 \* 1000\|86400000" lib/lesgroepen.ts` → 0 (geen milliseconden-rekenwerk voor het verzetten)
    - `grep -c "jest.mock\|jest.fn" lib/lesgroepen.test.ts` → 0
    - `grep -c "process.env.TZ = 'Europe/Brussels'" lib/lesgroepen.test.ts` → 1, op regel 1-3, vóór elke import
    - `npx jest lib/lesgroepen -t "planGroepWijziging" --silent` → minstens 12 tests groen
    - `npx jest lib/lesgroepen -t "uurwissel" --silent` → 1 test groen
    - `git diff lib/lesgroepen.test.ts | grep '^-' | grep -v '^---' | grep -c "expect"` → 0
    - `grep -v "^//" lib/lesgroepen.ts | grep -c "from '\.\./"` → 0
  </acceptance_criteria>

  <done>
    `planGroepWijziging` levert per komende les een patch of een melding met reden, laat het
    verleden en `taught_by_id` ongemoeid, houdt het lokale uur vast over de uurwissel, en de tests
    ernaast bewijzen elk van die zinnen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scherm → pure regel | Het scherm geeft een half ingevulde groepspatch mee terwijl de beheerder nog typt |
| plan → opslag | Wat dit plan berekent wordt in het volgende plan in één commit weggeschreven |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | Tampering | `planGroepWijziging` schrijft `taught_by_id` mee en herschrijft daarmee wie er loon krijgt | mitigate | `VerzetPatch` bevat het veld niet; grep-criterium + test die `'taught_by_id' in patch` op `false` zet |
| T-02.1-02 | Tampering | Een verzetting overschrijft een les die al gegeven is | mitigate | Uitsluitend `komendeLessen(..., now)`; test op een les van vóór `now` plus JSON-vergelijking |
| T-02.1-03 | Repudiation | Een les wordt stil over een bezette trainer of baan heen gezet | mitigate | `geblokkeerd` met reden en `conflict`-id; zulke lessen krijgen nooit een patch |
| T-02.1-04 | Information disclosure | — | accept | Deze module leest geen gebruikersgegevens en doet geen netwerkoproep |
| T-02.1-05 | Denial of service | Een groep met honderden lessen laat het scherm vastlopen | accept | Enkele tientallen komende lessen per groep (CONTEXT: ~32 per seizoen); één lus zonder opslag |
| T-02.1-SC | Tampering | npm/pip/cargo installs | accept | Dit plan installeert geen enkel pakket; er is geen `package.json`-wijziging |
</threat_model>

<verification>
- `npx tsc --noEmit` → schoon
- `npm test` → alle suites groen, met minstens 12 nieuwe tests erbij ten opzichte van de 1036 van fase 2
- `npx expo export --platform web` → geslaagd
- `git diff --stat providers/ app/ components/ supabase-schema.sql` → leeg (dit plan raakt alleen `lib/`)
- Geen SQL gedraaid, geen verbinding met Supabase
</verification>

<success_criteria>
- `botstMet` is geëxporteerd uit `lib/recurrence.ts`, kent trainer én baan én `negeer`, en is de
  enige implementatie van die vraag in `lib/`
- `planGroepWijziging` staat in `lib/lesgroepen.ts` met een test ernaast in dezelfde vorm als
  `planRosterChange`
- De vijf bewijzen bestaan als test: verleden ongemoeid, `taught_by_id` ongemoeid bij een
  wisselende `coach_id`, botsingen gemeld in plaats van geschreven, vakantie apart gemeld, en het
  lokale uur vastgehouden over de uurwissel
</success_criteria>

<output>
Create `.planning/phases/02.1-groep-verzetten/02.1-01-SUMMARY.md` when done
</output>
