---
phase: 05-excel-import-van-trainingen
plan: 07
type: execute
wave: 6
depends_on: [05-06]
files_modified:
  - lib/import-trainingen.test.ts
autonomous: true
requirements: [IMP-06, IMP-10]

must_haves:
  truths:
    - "`koen.xlsx` gaat ongewijzigd door de hele droogloop en levert de groepen met hun eigen roster op."
    - "De enige meldingen die het echte bestand oplevert gaan over de trainer en de baan die gekoppeld moeten worden."
    - "Hetzelfde echte bestand een tweede keer inlezen, tegen de toestand die de eerste keer opleverde, verandert niets."
    - "Wat de export van fase 4 schrijft, leest deze import ongewijzigd terug — inclusief Groep-ID."
  artifacts:
    - path: "lib/import-trainingen.test.ts"
      provides: "de acceptatietest van IMP-10 en de heen-en-terugtest met de export"
      contains: "IMP-10"
  key_links:
    - from: "lib/import-trainingen.test.ts"
      to: "koen.xlsx"
      via: "readFileSync op het echte bestand in de projectmap"
      pattern: "koen\\.xlsx"
    - from: "lib/import-trainingen.test.ts"
      to: "lib/export-trainingen.ts::bladLessen"
      via: "de heen-en-terugtest die EXP-07 waarmaakt"
      pattern: "bladLessen\\("
---

<objective>
De poort. Alles staat er; dit plan bewijst dat het klopt op het échte bestand van de club en op
wat de export van fase 4 schrijft. Geen nieuwe productiecode — alleen het bewijs.

Purpose: IMP-10 is de harde acceptatiebar van deze fase, en IMP-06 ("een tweede keer inlezen
verandert niets") is alleen echt bewezen als het op 1398 regels gebeurt en niet op drie
verzonnen rijen. En als de export van fase 4 niet teruggelezen kan worden, is EXP-07 een
belofte zonder dekking.
Output: twee `describe`-blokken in `lib/import-trainingen.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/IMPORT-SJABLOON.md
@.planning/phases/05-excel-import-van-trainingen/05-CONTEXT.md
@.planning/phases/04-excel-export/04-02-PLAN.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/05-excel-import-van-trainingen/05-06-SUMMARY.md

<verwachte_uitkomst>
<!-- Geteld op het échte bestand tijdens het plannen. Gebruik deze getallen letterlijk. -->

`koen.xlsx`: 1398 gegevensrijen, 9 september 2026 t/m 25 juni 2027, één coach `Leemans Koen`,
42 verschillende leerlingen, 325 verschillende lesmomenten, geen kolom `Baan` en geen kolom
`Groep-ID`.

Tien groepen volgens de sleutel uit D-02, met zeven verschillende namen:

| Groep | weekdag | uur | spelers | lesmomenten | niveau |
| --- | --- | --- | --- | --- | --- |
| Groep 4 | woensdag | 14:00 | 2 | 33 | Duoles |
| Groep 13 | woensdag | 16:00 | 6 | 33 | Oranje |
| Groep 8 | woensdag | 17:00 | 6 | 33 | Kidstennis oranje |
| Groep 26 | woensdag | 18:00 | 4 | 33 | Tienertennis geel |
| Groep 12 | woensdag | 15:00 | 4 | 33 | Tienertennis geel |
| Groep 31 | vrijdag | 16:00 | 5 | 32 | Tienertennis geel |
| Groep 8 | vrijdag | 17:00 | 4 | 32 | Tienertennis geel |
| Groep 12 | vrijdag | 18:00 | 4 | 32 | Privéles |
| Groep 8 | vrijdag | 19:00 | 2 | 32 | Duoles |
| Groep 3 | vrijdag | 20:00 | 6 | 32 | Volwassenen - (her)starters |

33 + 32 + ... = 325 lesmomenten in totaal. `Groep 12` op vrijdag 18:00 heeft `Type les` =
`Privéles` maar een gevulde `Groep`-kolom: dat is een niveau-etiket en géén privéles in de zin
van `.planning/IMPORT-SJABLOON.md` (dáár gaat het over een lege `Groep`). Bouw daar geen
uitzondering op.
</verwachte_uitkomst>

<tasks>

<task type="auto">
  <name>Taak 1: de acceptatietest van IMP-10 op het echte bestand</name>
  <files>lib/import-trainingen.test.ts</files>
  <read_first>
    - `.planning/phases/05-excel-import-van-trainingen/05-CONTEXT.md`, de sectie
      `<specifics>` — de letterlijke formulering van IMP-10.
    - `.planning/REQUIREMENTS.md`, IMP-10.
    - Het blok `<verwachte_uitkomst>` hierboven in zijn geheel.
    - `lib/import-trainingen.ts` zoals plan 05-06 hem achterliet — `planImportLessen` en de
      velden van `ImportPlanLessen`.
    - `lib/xlsx-lezen.test.ts` — de `koenBytes`-helper; hergebruik hem in plaats van een tweede
      te schrijven (trek hem naar een gedeeld testbestand als dat netter is, en zeg waarom).
    - Het blok `<besluit_trainer_ontbreekt>` in `.planning/phases/05-excel-import-van-trainingen/05-05-PLAN.md`.
  </read_first>
  <behavior>
    - Met een lege club (geen gebruikers, geen groepen, geen banen, geen boekingen, geen
      vakanties) levert `planImportLessen` op `koen.xlsx`:
      - `fouten` is leeg — geen enkele van de 1398 regels valt af;
      - `nietHerkend` is leeg — `Weekdag`, `Weeknr`, `Locatie` en `Indoor/Outdoor` geven geen ruis;
      - `dubbel` is leeg;
      - tien nieuwe groepen, met zeven verschillende namen;
      - elke groep heeft de weekdag, het uur, het aantal spelers en het niveau uit
        `<verwachte_uitkomst>`;
      - 42 nieuwe spelers;
      - nul nieuwe lessen — er is geen trainer gekoppeld en er is geen baan;
      - de meldingen gaan uitsluitend over de trainer `Leemans Koen` en over de ontbrekende
        baan. Controleer dat expliciet: elke melding bevat een van beide woorden, en er is er
        geen enkele over iets anders.
    - Met de trainer `Koen Leemans` in de ledenlijst en één baan gekoppeld — dus met een
      `Baan`-kolom die er niet is, dus via een tweede opzet waarin de groepen een baan
      meekrijgen langs de weg die het scherm daarvoor biedt, óf met de meldingen als bewijs dat
      dit de enige twee ontbrekende schakels zijn — verdwijnt de trainermelding. Kies één van
      beide en zeg in het commentaar waarom.
    - Elke groep heeft `seizoenVan` 2026-09-09 of 2026-09-11 en `seizoenTot` 2027-06-23 of
      2027-06-25, passend bij zijn weekdag.
    - De drie `Groep 8`-groepen hebben nul spelers gemeenschappelijk.
  </behavior>
  <action>
    Voeg één `describe('koen.xlsx — de acceptatie van IMP-10', ...)` toe aan
    `lib/import-trainingen.test.ts`, met een commentaarblok erboven dat zegt waarom dit de
    zwaarste test van de fase is: het is de enige die het echte bestand van de club van begin
    tot eind door de hele keten haalt, en de enige die zou omvallen als een van de vijf lagen
    eronder stilletjes een uur, een dag of een groep verschuift.

    Lees het bestand één keer voor het hele blok. Roep `planImportLessen` één keer aan en doe
    de beweringen uit `<behavior>` op dat ene plan — niet één aanroep per bewering; 1398 rijen
    × twaalf tests is verspilling.

    Zet de tabel uit `<verwachte_uitkomst>` als een gegevensstructuur bovenaan het blok en loop
    er in één test overheen, zodat een verschoven groep meteen zegt wélke groep.

    Voeg géén productiecode toe in dit plan. Blijkt er iets niet te kloppen, dan hoort de fix
    in het plan waar die logica woont — en dan is dat precies wat deze test moest opleveren.
  </action>
  <verify>
    <automated>cd "/Users/leko/Downloads/tennis app" && npx jest lib/import-trainingen && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest lib/import-trainingen` slaagt.
    - `grep -c 'IMP-10' lib/import-trainingen.test.ts` is minstens `1`.
    - `grep -c 'Kidstennis oranje' lib/import-trainingen.test.ts` is minstens `1`.
    - `grep -c 'Volwassenen - (her)starters' lib/import-trainingen.test.ts` is minstens `1`.
    - `grep -c '325' lib/import-trainingen.test.ts` is minstens `1`.
    - `git diff --stat lib/import-trainingen.ts` is leeg — dit plan voegt geen productiecode toe.
    - `npx tsc --noEmit` geeft nul fouten.
  </acceptance_criteria>
  <done>
    Het echte bestand van de club gaat van bytes tot plan zonder één fout, levert tien groepen
    met de juiste rosters en niveaus op, en de enige meldingen gaan over de trainer en de baan.
  </done>
</task>

<task type="auto">
  <name>Taak 2: twee keer inlezen, en de export weer inlezen (IMP-06, EXP-07)</name>
  <files>lib/import-trainingen.test.ts</files>
  <read_first>
    - `lib/export-trainingen.ts` in zijn geheel — vooral `LESSEN_KOLOMMEN` (de zestien koppen)
      en `bladLessen`. Dit bestaat al; het is de tegenoverliggende helft van deze fase.
    - `.planning/phases/04-excel-export/04-02-PLAN.md`, het blok `<column_contract>`.
    - `lib/xlsx.ts` regel 369-401 (`buildXlsx`) — hoe een blad een bestand wordt.
    - `.planning/IMPORT-SJABLOON.md`, sectie "Herimport".
  </read_first>
  <behavior>
    - Eerste ronde: `planImportLessen` op `koen.xlsx` tegen een lege club. Bouw daaruit met de
      hand de toestand die de import zou opleveren: de tien groepen als `LesGroep` met ids en
      rosters, de 42 spelers als `User` met ids.
    - Tweede ronde: `planImportLessen` op exact hetzelfde bestand tegen díé toestand levert nul
      nieuwe groepen, nul nieuwe spelers en nul nieuwe lessen op; alle tien de groepen staan in
      `groepenOngewijzigd`.
    - Een derde ronde tegen dezelfde toestand geeft hetzelfde antwoord als de tweede — het is
      stabiel, niet toevallig.
    - Heen en terug met de export: bouw een handvol `Booking`s van een lesgroep, laat
      `bladLessen` er een blad van maken, schrijf dat met `buildXlsx` naar bytes, lees het met
      `leesWerkmap` + `kiesLessenBlad` + `planImportLessen` weer in, en controleer dat de
      lesgroep, de dag, het uur, het niveau, de trainer, de baan en het volledige roster er
      ongewijzigd uitkomen.
    - In diezelfde heen-en-terugtest: omdat de export `Groep-ID` invult, wordt de bestaande
      lesgroep herkend ook als zijn naam in het bestand veranderd is (D-03) — pas de naam in
      het blad aan en controleer dat de groep als `bijgewerkt` en niet als `nieuw` binnenkomt.
    - Een les uit de export met `Type les` = `Privéles` en een lege `Groep` levert geen
      lesgroep op.
  </behavior>
  <action>
    Voeg twee `describe`-blokken toe: `'koen.xlsx twee keer inlezen'` en
    `'heen en terug met de export van fase 4'`.

    Voor het eerste blok: schrijf een kleine hulpfunctie in het testbestand die uit een
    `ImportPlanLessen` de toestand samenstelt die de uitvoerder eruit zou maken (groepen met
    verzonnen ids, spelers met verzonnen ids). Zet er een commentaar bij dat dit met opzet met
    de hand gebeurt en niet via de uitvoerder van plan 05-08: die schrijft, en deze test moet
    puur blijven. Dit is dezelfde reden waarom `planImportLessen` puur is.

    Voor het tweede blok: dit is de test die EXP-07 en IMP-06 aan elkaar knoopt. Zet er een
    commentaar bij dat het hier niet om Excel gaat maar om een belofte: de club exporteert een
    seizoen, past er iets in aan, en leest het weer in — en dat mag niets verdubbelen. Bouw de
    boekingen met lokale datums.

    Voeg géén productiecode toe. Blijkt de export een kop anders te spellen dan de import
    verwacht, dan is dat een echte bevinding: meld hem in de samenvatting van dit plan in
    plaats van hem in de test glad te strijken.
  </action>
  <verify>
    <automated>cd "/Users/leko/Downloads/tennis app" && npx jest lib/import-trainingen lib/export-trainingen && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest lib/import-trainingen` slaagt.
    - `grep -c 'bladLessen' lib/import-trainingen.test.ts` is minstens `1`.
    - `grep -c 'Groep-ID' lib/import-trainingen.test.ts` is minstens `1`.
    - `grep -c 'twee keer inlezen' lib/import-trainingen.test.ts` is minstens `1`.
    - `git diff --stat lib/import-trainingen.ts lib/export-trainingen.ts lib/xlsx.ts` is leeg.
    - `npm test` slaagt volledig; `npx tsc --noEmit` geeft nul fouten.
  </acceptance_criteria>
  <done>
    Hetzelfde bestand twee en drie keer inlezen verandert niets, en een export van fase 4 gaat
    ongewijzigd weer naar binnen — met `Groep-ID` als de herkenning die een hernoemde groep
    heel houdt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| geen nieuwe | Dit plan voegt alleen tests toe. Geen scherm, geen netwerk, geen tabel, geen SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Tampering | een kolom die tussen export en import uit elkaar loopt | mitigate | De heen-en-terugtest leest wat `bladLessen` schrijft; hernoemt iemand een kop aan één kant, dan valt deze test om (taak 2) |
| T-05-19 | Repudiation | een acceptatietest die stilletjes minder controleert dan hij belooft | mitigate | De verwachte uitkomst staat als tabel in dit plan met getallen die op het echte bestand geteld zijn; de test loopt die tabel af (taak 1) |
| T-05-SC | Tampering | npm-installs | mitigate | Dit plan installeert niets; `git diff --stat package.json package-lock.json` moet leeg zijn |
</threat_model>

<verification>
```bash
cd "/Users/leko/Downloads/tennis app"
npx tsc --noEmit
npm test
npx expo export --platform web --output-dir .webbuild-check && rm -rf .webbuild-check
git diff --stat package.json package-lock.json   # moet leeg zijn
git diff --name-only lib/ | grep -v 'test' || true   # alleen testbestanden mogen gewijzigd zijn
```
</verification>

<success_criteria>
- `koen.xlsx` gaat ongewijzigd door de hele keten, levert tien groepen met zeven namen op, en
  de enige meldingen gaan over de trainer en de baan (IMP-10).
- Twee en drie keer hetzelfde bestand inlezen verandert niets (IMP-06).
- Wat de export schrijft, leest de import terug, inclusief `Groep-ID` (EXP-07/D-03).
- Er is geen productiecode aan `lib/` toegevoegd in dit plan.
- `npx tsc --noEmit`, `npm test` en `npx expo export --platform web` slagen.
</success_criteria>

<output>
Create `.planning/phases/05-excel-import-van-trainingen/05-07-SUMMARY.md` when done
</output>
