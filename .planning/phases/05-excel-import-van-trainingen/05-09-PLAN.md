---
phase: 05-excel-import-van-trainingen
plan: 09
type: execute
wave: 8
depends_on: [05-08]
files_modified:
  - app/admin/trainingen-import.tsx
  - app/admin/index.tsx
  - app/_layout.tsx
  - lib/i18n-en.ts
autonomous: false
requirements: [IMP-01, IMP-02, IMP-09]

must_haves:
  truths:
    - "De beheerder vindt \"Trainingen importeren\" in Beheer en niemand anders ziet het."
    - "Hij downloadt een leeg sjabloon dat de verwachte kolommen toont."
    - "Hij kiest een bestand en ziet vóór er iets wegschrijft welke groepen, spelers en lessen erbij komen — in groepen en aantallen, niet in 1400 regels."
    - "Pas na een uitdrukkelijke bevestiging wordt er iets weggeschreven."
    - "Het scherm zegt eerlijk wat er gebeurt als een import halverwege mislukt: hetzelfde bestand opnieuw inlezen maakt het af."
  artifacts:
    - path: "app/admin/trainingen-import.tsx"
      provides: "het importscherm met droogloop, bevestiging en opnieuw proberen"
      contains: "planImportLessen"
    - path: "lib/i18n-en.ts"
      provides: "de Engelse tegenhangers van de nieuwe schermteksten"
      contains: "Trainingen importeren"
  key_links:
    - from: "app/admin/trainingen-import.tsx"
      to: "lib/import-trainingen.ts::planImportLessen"
      via: "het scherm rekent zelf niets uit"
      pattern: "planImportLessen\\("
    - from: "app/admin/trainingen-import.tsx"
      to: "lib/bestand.ts::kiesBinairBestand"
      via: "een .xlsx als bytes"
      pattern: "kiesBinairBestand\\("
    - from: "app/admin/index.tsx"
      to: "app/admin/trainingen-import.tsx"
      via: "de tegel in Beheer"
      pattern: "trainingen-import"
---

<objective>
Het scherm. Alles eronder is af en getest; hier wordt het bruikbaar — en hier wordt de belofte
van IMP-02 zichtbaar: je ziet het vóór het gebeurt.

Purpose: de droogloop is alleen iets waard als hij leesbaar is. 1398 regels zijn geen
droogloop; tien groepen met hun dag, uur, trainer en aantal spelers wél (D-09).
Output: `app/admin/trainingen-import.tsx`, de tegel in Beheer, de route, en de Engelse teksten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/IMPORT-SJABLOON.md
@.planning/phases/05-excel-import-van-trainingen/05-CONTEXT.md
@.planning/phases/05-excel-import-van-trainingen/05-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@.planning/phases/05-excel-import-van-trainingen/05-08-SUMMARY.md

<interfaces>
<!-- Alles wat het scherm aanroept. Het scherm rekent niets uit. -->

// lib/import-trainingen.ts
export function planImportLessen(rijen, bestaandeGroepen, users, courts, bookings, settings, nu): ImportPlanLessen;
export function bestandAfgekeurdLessen(plan: ImportPlanLessen): boolean;
export function voorbeeldTrainingenXlsx(): Uint8Array;

// lib/xlsx-lezen.ts
export function leesWerkmap(bytes: Uint8Array): GelezenBlad[];
// lib/import-trainingen.ts
export function kiesLessenBlad(bladen: readonly GelezenBlad[]): GelezenBlad | null;

// lib/bestand.ts
export const kanBestandKiezen: boolean;
export function kiesBinairBestand(): Promise<{ naam: string; bytes: Uint8Array } | null>;

// lib/share.ts
export const xlsxWordtOndersteund: boolean;
export async function shareXlsx(filename: string, bytes: Uint8Array): Promise<void>;

// lib/rechten.ts
export function isAdmin(u: User | null | undefined): boolean;   // controleer de exacte naam

// providers/SimpleDataProvider.tsx (uit plan 05-08)
importeerTrainingen: (plan: ImportPlanLessen) => Promise<ImportUitslagLessen>;
</interfaces>

<tasks>

<task type="auto">
  <name>Taak 1: het scherm met de droogloop</name>
  <files>app/admin/trainingen-import.tsx</files>
  <read_first>
    - `app/admin/leden-import.tsx` in zijn geheel (427 regels). Dit is de analoog en het wordt
      hier gespiegeld: regel 19-54 (de moduleniveau-toestand die een her-mount overleeft),
      100-120 (de beheerdersgrens op het scherm zelf), 162-182 (de bestandskeuze en de
      terugval), 230-256 (het afgekeurde bestand als eigen scherm) en 256-388 (de plansamenvatting).
    - `components/ui/Screen.tsx`, `components/ui/Card.tsx`, `components/ui/StatCard.tsx`,
      `components/ui/ActionTile.tsx` — de bestaande bouwstenen; verzin geen nieuwe visuele
      patronen (CONVENTIONS.md, "Reusable UI Primitives — Do Not Reinvent").
    - `lib/rechten.ts` in zijn geheel — de beheerdersgrens, en het commentaar dat zegt dat de
      databank de echte bewaker is.
    - `.planning/IMPORT-SJABLOON.md`, sectie "De droogloop" — precies wat er getoond moet worden.
    - `.planning/codebase/CONCERNS.md`, de sectie over "Confirm email" vóór een bulk-import.
  </read_first>
  <action>
    Maak `app/admin/trainingen-import.tsx`, naar het model van `app/admin/leden-import.tsx`:

    - De moduleniveau-toestand die een her-mount overleeft, verbatim gespiegeld met
      `ImportUitslagLessen`-getypeerde gebeurtenissen. Bij 325 lessen telt dit zwaarder dan bij
      een ledenlijst: een beheerder die tijdens het wegschrijven wegnavigeert mag geen tweede
      beurt over hetzelfde bestand krijgen. Neem het commentaar mee dat dat uitlegt.
    - De beheerdersgrens op het scherm zelf, in dezelfde vorm als `leden-import.tsx` (TOEG-01),
      met het commentaar dat dit de beleefde grens is en de databank de echte.
    - Een knop "Voorbeeldbestand downloaden" die `voorbeeldTrainingenXlsx()` door `shareXlsx`
      haalt (IMP-01). Verberg hem als `xlsxWordtOndersteund` onwaar is.
    - Een knop "Bestand kiezen" die `kiesBinairBestand()` aanroept, de bytes door `leesWerkmap`
      + `kiesLessenBlad` haalt en daarna `planImportLessen` met de huidige lijsten uit de
      provider. Vangt een fout uit de lezer op en toont die als één melding over het hele
      bestand — precies zoals `bestandAfgekeurd` in de ledenimport een eigen schermtoestand is.
    - Is `kanBestandKiezen` onwaar (een telefoon), toon dan een uitleg in plaats van een knop:
      een xlsx kan niet geplakt worden, dus hier is geen terugval zoals bij de ledenimport wél
      bestaat. Zet die reden als commentaar én in de schermtekst.
    - De samenvatting, in deze volgorde en in aantallen, nooit in regels (D-09):
      1. lesgroepen: nieuw / bijgewerkt / ongewijzigd — per groep de naam, de dag, het uur, de
         trainer en het aantal spelers;
      2. spelers: hoeveel nieuw, met hun namen;
      3. lessen: hoeveel ingepland, hoeveel overgeslagen wegens een clubvakantie, hoeveel
         botsend met een bezette trainer of baan;
      4. met de hand verzette of afgezegde lessen die de import níét terugzet, met de dag en
         beide tijden — dit is het lijstje waarover de beheerder beslist (IMP-08);
      5. lessen die wel in de app staan maar niet meer in het bestand, als melding;
      6. wat niet gelezen kon worden, met regelnummer en reden.
    - Elke reden loopt door `t(reden, vars)` — de meldingen uit `lib/` zijn vaste zinnen met
      `{plaatshouders}` en worden hier pas ingevuld.

    Alle schermtekst is Nederlands en gaat door `t()`. De koppen van het sjabloonbestand niet:
    die komen uit `lib/` en zijn een bestandsformaat.

    Schrijf in deze taak nog geen bevestigings- of uitvoerknop.
  </action>
  <verify>
    <automated>cd "/Users/leko/Downloads/tennis app" && npx tsc --noEmit && npm test && npx expo export --platform web --output-dir .webbuild-check && rm -rf .webbuild-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'planImportLessen' app/admin/trainingen-import.tsx` is minstens `1`.
    - `grep -c 'kiesBinairBestand' app/admin/trainingen-import.tsx` is minstens `1`.
    - `grep -c 'isAdmin' app/admin/trainingen-import.tsx` is minstens `1`.
    - `grep -c 'importDraait\|laatsteUitslag' app/admin/trainingen-import.tsx` is minstens `2`.
    - `grep -cE '\.filter\(|\.reduce\(|groepSleutel|botstMet|vakantieOp' app/admin/trainingen-import.tsx` geeft `0` — het scherm rekent niets uit.
    - `grep -c 'voorbeeldTrainingenXlsx' app/admin/trainingen-import.tsx` is minstens `1`.
    - `npx tsc --noEmit` geeft nul fouten; `npm test` slaagt; de webbuild slaagt.
  </acceptance_criteria>
  <done>
    De beheerder kiest een bestand en ziet de volledige droogloop in groepen en aantallen,
    zonder dat er iets weggeschreven is.
  </done>
</task>

<task type="auto">
  <name>Taak 2: bevestigen, wegschrijven, opnieuw proberen — en het eerlijk zeggen</name>
  <files>app/admin/trainingen-import.tsx, app/admin/index.tsx, app/_layout.tsx, lib/i18n-en.ts</files>
  <read_first>
    - `app/admin/leden-import.tsx` regel 300-390 — de bevestigingsknop, de voortgang, de
      uitslag en de knop "Opnieuw proberen" die het plan opnieuw uitrekent tegen de intussen
      bijgewerkte lijst. Dat laatste is D-21 op het scherm.
    - `app/admin/index.tsx` regel 70-90 — de tegels in Beheer, en hoe "Leden importeren" erin staat.
    - `app/_layout.tsx` regel 90-100 — hoe een schermtitel geregistreerd wordt.
    - `lib/i18n-en.ts` rond regel 900 — hoe `'Leden importeren'` erin staat.
    - `.planning/phases/05-excel-import-van-trainingen/05-CONTEXT.md`, D-21 letterlijk:
      "Zeg het ook zo tegen de gebruiker op het scherm."
  </read_first>
  <action>
    Voeg toe aan het scherm:

    - Een knop "Importeren" die pas iets doet na een uitdrukkelijke bevestiging, en die
      uitgeschakeld staat zolang er niets nieuws in het plan zit — dezelfde vorm als
      `leden-import.tsx`. Hij roept `importeerTrainingen(plan)` aan, één keer, en toont de
      voortgang.
    - Onder die knop, vóór het bevestigen, één alinea die eerlijk zegt wat er gebeurt als het
      halverwege misgaat. In gewone taal, ongeveer: er worden eerst de spelers weggeschreven,
      dan de groepen, dan de lessen. Gaat er onderweg iets mis, dan blijft staan wat er al
      stond en is er niets dubbel — hetzelfde bestand nog een keer inlezen maakt het af.
      Geen belofte over een transactie die er niet is (D-21).
    - Een knop "Opnieuw proberen" na een mislukking, die het plan opnieuw uitrekent tegen de
      intussen bijgewerkte lijsten en dus toont wat er nog openstaat. Neem het commentaar uit
      `leden-import.tsx` over waarom dat zo werkt mee.
    - Boven de importknop een korte herinnering dat "Confirm email" in Supabase aan hoort te
      staan vóór er in bulk spelers aangemaakt worden — dezelfde operationele afspraak als bij
      de ledenimport, vastgelegd in `.planning/codebase/CONCERNS.md`.

    Voeg de tegel toe aan `app/admin/index.tsx`, naast "Leden importeren": titel "Trainingen
    importeren", ondertitel "Een seizoen uit Excel", hetzelfde `Upload`-pictogram of een
    passend ander uit `lucide-react-native`, en `router.push('/admin/trainingen-import')`.

    Registreer de route in `app/_layout.tsx` in dezelfde vorm als `admin/leden-import`.

    Voeg voor élke nieuwe Nederlandse schermtekst een Engelse tegenhanger toe aan
    `lib/i18n-en.ts`, gesorteerd zoals de buren daar staan. De koppen van het sjabloonbestand
    horen daar niet bij — die staan in `lib/` en zijn een bestandsformaat.
  </action>
  <verify>
    <automated>cd "/Users/leko/Downloads/tennis app" && npx tsc --noEmit && npm test && npx expo export --platform web --output-dir .webbuild-check && rm -rf .webbuild-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'importeerTrainingen' app/admin/trainingen-import.tsx` is minstens `1`.
    - `grep -c 'trainingen-import' app/admin/index.tsx` is minstens `1` en `grep -c 'trainingen-import' app/_layout.tsx` is minstens `1`.
    - `grep -c 'Trainingen importeren' lib/i18n-en.ts` is minstens `1`.
    - Elke `t('...')`-tekst in het nieuwe scherm heeft een Engelse tegenhanger. Controleer met:
      `grep -o "t('[^']*'" app/admin/trainingen-import.tsx | sed "s/t('//;s/'$//" | sort -u | while read -r s; do grep -qF "'$s'" lib/i18n-en.ts || echo "GEEN ENGELS: $s"; done` — dat geeft geen uitvoer.
    - `grep -ci 'opnieuw' app/admin/trainingen-import.tsx` is minstens `1`.
    - `grep -ci 'confirm email' app/admin/trainingen-import.tsx` is minstens `1`.
    - `npx tsc --noEmit` geeft nul fouten; `npm test` slaagt; de webbuild slaagt.
  </acceptance_criteria>
  <done>
    De import wordt pas na een bevestiging uitgevoerd, het scherm zegt eerlijk wat een halve
    mislukking betekent en biedt "opnieuw proberen", en de tegel staat in Beheer met een
    Engelse vertaling voor elke nieuwe zin.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Taak 3: met eigen ogen, zonder de echte club te raken</name>
  <what-built>
    Het volledige importscherm: sjabloon downloaden, een bestand kiezen, de droogloop lezen,
    bevestigen, en de uitslag zien. Alles eronder is met tests gedekt; wat een test niet ziet
    is of het scherm leesbaar is en of de knoppen doen wat ze zeggen.
  </what-built>
  <how-to-verify>
    **Vóór je begint: de dev-server praat met de echte Supabase van de club.** Zet daarom eerst
    de omgeving uit, zodat de app op de mock-opslag draait:

    1. Hernoem `.env` tijdelijk naar `.env.uit` (of hoe het bestand met de Supabase-sleutels
       ook heet — controleer eerst welke er staat) en start de webserver opnieuw:
       `npx expo start --web --clear`. Controleer dat de app op de voorbeeldgegevens draait
       vóór je verdergaat.
    2. Meld je aan als beheerder en ga naar Beheer. Staat "Trainingen importeren" er als tegel?
    3. Meld je aan als een gewone trainer. Is de tegel weg, en geeft het rechtstreeks openen van
       `/admin/trainingen-import` een nette weigering?
    4. Terug als beheerder: klik "Voorbeeldbestand downloaden". Open het in Excel. Staan de
       negen kolommen erin, en zijn er twee voorbeeldregels?
    5. Kies `koen.xlsx` uit de projectmap. Verschijnt de droogloop binnen enkele seconden?
       Staan er tien lesgroepen met hun dag, uur en aantal spelers? Staan er 42 nieuwe spelers?
       Staat er nul ingeplande lessen, met als enige melding dat de trainer en de baan gekoppeld
       moeten worden?
    6. Lees de alinea over wat er gebeurt als het halverwege misgaat. Klopt hij, en is hij te
       begrijpen zonder de code te kennen?
    7. Bevestig de import. Ga naar Beheer → Lesgroepen. Staan de tien groepen er, met hun
       rosters?
    8. Ga terug naar het importscherm en kies `koen.xlsx` opnieuw. Zegt de droogloop nu dat er
       niets nieuws is — nul nieuwe groepen, nul nieuwe spelers, nul nieuwe lessen?
    9. Zet `.env` terug zoals het stond en start de server opnieuw.

    Zeg per punt of het klopte. Wat niet klopte, met wat je zag.
  </how-to-verify>
  <resume-signal>Typ "goedgekeurd", of beschrijf wat er misging</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gebruiker → scherm | Alleen een beheerder mag hier komen. Het scherm is de beleefde grens; RLS is de echte. |
| scherm → provider | Het scherm geeft een plan door dat het niet zelf heeft uitgerekend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Elevation of Privilege | een trainer die het importscherm opent | mitigate | `isAdmin`-grens op het scherm zelf, in dezelfde vorm als `leden-import.tsx` (TOEG-01); RLS blijft de echte bewaker |
| T-05-25 | Spoofing | in bulk aangemaakte spelersrijen zijn claimbaar vóór het echte lid zich aanmeldt | transfer | Herinnering op het scherm om "Confirm email" aan te zetten, zoals bij de ledenimport (CONCERNS.md) |
| T-05-26 | Denial of Service | twee keer op "Importeren" drukken, of wegnavigeren tijdens het wegschrijven | mitigate | De moduleniveau-toestand `importDraait` overleeft een her-mount en houdt een tweede beurt tegen (taak 1) |
| T-05-27 | Tampering | testen op de dev-server wijzigt productiedata van de club | mitigate | Het handmatige nalopen gebeurt met de omgeving uitgezet, op de mock-opslag (taak 3, stap 1 en 9) |
| T-05-SC | Tampering | npm-installs | mitigate | Dit plan installeert niets; `git diff --stat package.json package-lock.json` moet leeg zijn |
</threat_model>

<verification>
```bash
cd "/Users/leko/Downloads/tennis app"
npx tsc --noEmit
npm test
npx expo export --platform web --output-dir .webbuild-check && rm -rf .webbuild-check
git diff --stat package.json package-lock.json   # moet leeg zijn
```
</verification>

<success_criteria>
- De beheerder downloadt een sjabloon en ziet een volledige droogloop vóór er iets wegschrijft
  (IMP-01, IMP-02).
- Er wordt pas weggeschreven na een uitdrukkelijke bevestiging.
- Het scherm zegt eerlijk wat een halve mislukking betekent en biedt "opnieuw proberen" (IMP-09).
- Alleen een beheerder komt op het scherm.
- Elke nieuwe Nederlandse zin heeft een Engelse tegenhanger.
- Geen nieuw pakket, geen SQL, geen schrijfbeurt naar de echte club.
- `npx tsc --noEmit`, `npm test` en `npx expo export --platform web` slagen.
</success_criteria>

<output>
Create `.planning/phases/05-excel-import-van-trainingen/05-09-SUMMARY.md` when done
</output>
