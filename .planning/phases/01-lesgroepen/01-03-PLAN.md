---
phase: 01-lesgroepen
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/sync.ts
  - lib/sync.test.ts
  - providers/mockStore.ts
  - providers/supabaseStore.ts
  - providers/SimpleDataProvider.tsx
autonomous: true
requirements: [GROEP-01, GROEP-02, GROEP-05, GROEP-06, GROEP-07]
must_haves:
  truths:
    - "Een lesgroep bewaren werkt in mock-modus én in Supabase-modus"
    - "Een club die de migratie nog niet draaide laadt gewoon door, met een lege lijst groepen"
    - "Een opslag van vóór de lesgroepen levert een lege lijst op in plaats van undefined"
    - "Een roosterwijziging schrijft de groep en de geraakte toekomstige lessen in één stap weg"
  artifacts:
    - path: "lib/sync.ts"
      provides: "'lesGroepen' als SyncTable, in SyncableStore, in de before-terugval en in diffStores"
      contains: "lesGroepen"
    - path: "providers/mockStore.ts"
      provides: "lesGroepen in StoreData, freshSeed en withDefaults"
      contains: "lesGroepen: data.lesGroepen ?? []"
    - path: "providers/supabaseStore.ts"
      provides: "lesGroepen -> lesson_groups, geladen met selectAllOptioneel"
      contains: "selectAllOptioneel"
    - path: "providers/SimpleDataProvider.tsx"
      provides: "addLesGroep, updateLesGroep, updateLesGroepRoster, archiveLesGroep"
      exports: ["addLesGroep", "updateLesGroep", "updateLesGroepRoster", "archiveLesGroep"]
  key_links:
    - from: "providers/supabaseStore.ts"
      to: "lesson_groups"
      via: "TABLES-map en selectAllOptioneel"
      pattern: "lesGroepen: 'lesson_groups'"
    - from: "providers/SimpleDataProvider.tsx"
      to: "lib/lesgroepen.ts"
      via: "planRosterChange in updateLesGroepRoster"
      pattern: "planRosterChange"
    - from: "lib/sync.ts"
      to: "diffStores"
      via: "changeFor op lesGroepen"
      pattern: "changeFor\\('lesGroepen'"
---

<objective>
De lesgroep door beide opslagwegen wiren — mock én Supabase — plus de vier provideracties die
de schermen straks aanroepen.

Purpose: "beide of geen van beide". Half wiren breekt de app in precies één modus, en dat
merk je pas laat: als `lib/sync.ts` de nieuwe tabel niet kent, lijkt alles lokaal te werken
terwijl er tegen Supabase nooit iets vertrekt. Dit plan is de vierstopscontrole uit
PATTERNS.md, met `relaties`/`ouder_kind` als het sjabloon dat het meest recent zo is gebouwd.
Output: een lesgroep die je kan aanmaken, wijzigen, van rooster veranderen en archiveren,
in beide modi, in één snapshot per actie.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-lesgroepen/01-CONTEXT.md
@.planning/phases/01-lesgroepen/01-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/01-lesgroepen/01-01-SUMMARY.md

<interfaces>
Uit plan 01, beschikbaar en getest:

  lib/types.ts:   LesGroep, Booking.group_id, Settings.lesson_duration_minutes
  lib/lesgroepen.ts:
    planRosterChange(group, newRoster, bookings, now) -> { group, bookingPatches }
    lesGroepFout(g) -> string | null
    komendeLessen / groupBookingsFrom / lessenVanGroep / actieveGroepen / gearchiveerdeGroepen

De naam van de verzameling in de opslag is `lesGroepen`; de tabel in Supabase heet
`lesson_groups`; het id-voorvoegsel is `lg`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Taak 1: lib/sync.ts kent lesGroepen — op alle vier de plekken</name>
  <files>lib/sync.ts, lib/sync.test.ts</files>
  <read_first>
    - `lib/sync.ts` in zijn geheel (149 regels). De vier plekken die `relaties` raakt: het
      `import type`-blok bovenaan, de `SyncTable`-union (regels 22-24), `SyncableStore`
      (regels 51-64), de `before`-terugval binnen `diffStores` (regels 118-122) en de
      `tables`-array (regels 124-134).
    - `lib/sync.test.ts` — de bestaande vorm van de tests over `diffStores`.
    - `.planning/phases/01-lesgroepen/01-PATTERNS.md` sectie `lib/sync.ts`, waar alle vier de
      plekken verbatim staan met wat eraan toegevoegd moet worden.
  </read_first>
  <action>
    Voeg `'lesGroepen'` toe aan de `SyncTable`-union, `lesGroepen: LesGroep[]` aan
    `SyncableStore` met een eigen eenregelig Nederlands commentaar, `lesGroepen: []` aan het
    `before`-terugvalobject in `diffStores`, en `changeFor('lesGroepen', before.lesGroepen,
    next.lesGroepen)` aan de `tables`-array. Importeer `LesGroep` uit `./types` in het
    bestaande `import type`-blok.

    De terugval is geen detail: zonder `lesGroepen: []` daar denkt een allereerste bewaaractie
    dat er niets bestond in plaats van dat er niets in stond, en dan verdwijnt het verschil.
    Zet dat als reden in het commentaar.

    Voeg aan `lib/sync.test.ts` twee gevallen toe: een nieuwe lesgroep verschijnt als upsert op
    de tabel `lesGroepen`, en een verdwenen lesgroep verschijnt daar als `remove`.
  </action>
  <verify>
    <automated>npx jest lib/sync && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lesGroepen" lib/sync.ts` geeft minstens 4.
    - `grep -c "changeFor('lesGroepen'" lib/sync.ts` geeft 1.
    - De `before`-terugval in `diffStores` bevat `lesGroepen: []`.
    - `npx jest lib/sync` slaagt en bevat een test die bewijst dat een nieuwe lesgroep als
      upsert op tabel `lesGroepen` in de `StoreChange` komt, en een die bewijst dat een
      verdwenen lesgroep als `remove` verschijnt.
    - `npx tsc --noEmit` is schoon.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Taak 2: beide opslagwegen — mockStore en supabaseStore</name>
  <files>providers/mockStore.ts, providers/supabaseStore.ts</files>
  <read_first>
    - `providers/mockStore.ts` in zijn geheel (120 regels): `StoreData` (regels 18-33),
      `freshSeed` (regels 35-49), `withDefaults` (regels 56-72) met de regel
      `relaties: data.relaties ?? []` en het commentaar "Een opslag van vóór de ouderkoppeling
      heeft dit veld niet.", en `newId` (regel 84).
    - `providers/supabaseStore.ts`: de `TABLES`-map (regels 30-41), `loadFromSupabase`
      (regels 133-177) met het `Promise.all`-blok, het commentaar "Deze twee kwamen later dan
      de rest van het schema", het gebruik van `selectAllOptioneel` voor `ouder_kind` en
      `coach_rates`, en de `saveToSupabase`-lus over `change.tables` (regels 271-286).
    - `.planning/phases/01-lesgroepen/01-PATTERNS.md` secties `providers/mockStore.ts` en
      `providers/supabaseStore.ts` — beide analogen staan er verbatim.
    - `.planning/phases/01-lesgroepen/01-CONTEXT.md` D-11.
  </read_first>
  <action>
    In `providers/mockStore.ts`: voeg `lesGroepen: LesGroep[]` toe aan `StoreData` met een
    eenregelig Nederlands commentaar dat naar `lib/types: LesGroep` verwijst, `lesGroepen: []`
    aan `freshSeed()` (geen zaaigegevens — een nieuwe club begint zonder groepen), en
    `lesGroepen: data.lesGroepen ?? []` aan `withDefaults()` met het commentaar "Een opslag van
    vóór de lesgroepen heeft dit veld niet." Die laatste is de belangrijkste van de drie: zonder
    hem krijgt elk scherm dat `.lesGroepen.map(...)` doet `undefined` bij elke club waarvan de
    lokale opslag ouder is dan dit veld.

    In `providers/supabaseStore.ts`: voeg `lesGroepen: 'lesson_groups'` toe aan de `TABLES`-map,
    importeer `LesGroep`, en laad de tabel in het bestaande `Promise.all` met
    `selectAllOptioneel<LesGroep>('lesson_groups')` — niet met `selectAll`. Breid het bestaande
    "Deze kwamen later dan de rest van het schema"-commentaar uit zodat `lesson_groups` erbij
    hoort, met de reden erbij: de gebruiker draait de migratie zelf en heeft dat op het moment
    van deze code misschien nog niet gedaan (D-11); dan hoort de hele lading niet stuk te lopen
    maar hoort er gewoon nog geen groep te bestaan. Neem `lesGroepen` op in het teruggegeven
    `StoreData`-object.

    Aan `saveToSupabase` verandert niets: de lus over `change.tables` handelt elke `SyncTable`
    al uniform af via `TABLES[table]`. Controleer dat en verander er niets aan.

    Maak geen verbinding met Supabase en draai geen enkele query tijdens deze taak.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lesGroepen" providers/mockStore.ts` geeft 3.
    - `grep "lesGroepen: data.lesGroepen ?? \[\]" providers/mockStore.ts` vindt een regel, met
      een Nederlands commentaar op de regel erboven.
    - `grep "lesGroepen: 'lesson_groups'" providers/supabaseStore.ts` vindt een regel.
    - `grep "selectAllOptioneel<LesGroep>('lesson_groups')" providers/supabaseStore.ts` vindt
      een regel; `grep "selectAll<LesGroep>('lesson_groups')"` vindt niets.
    - `lesGroepen` staat in het object dat `loadFromSupabase` teruggeeft.
    - `git diff providers/supabaseStore.ts` toont geen wijziging binnen `saveToSupabase`.
    - `npx tsc --noEmit` schoon, `npm test` groen.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Taak 3: de vier provideracties, elk één commit</name>
  <files>providers/SimpleDataProvider.tsx</files>
  <read_first>
    - `providers/SimpleDataProvider.tsx`: `addBooking` (regels 558-575, de vorm van aanmaken
      met validatie vóór commit en `newId`), `updateBooking` (regels 702-720, de vorm van
      patchen), de relatie-acties `vraagKindAan`/`beslisOverKind`/`wisRelatie` (regels 952-988),
      de twee plekken waar twee verzamelingen in één `commit()` gaan (rond regel 653 en regel
      894), de terugval voor de instellingen (regel 1113), de context-waarde (rond regels
      1128-1169) en de `useMemo`-afhankelijkheidslijst daaronder. Lees ook het `interface`-blok
      van de context bovenaan (rond regel 85, `updateBooking`).
    - `lib/lesgroepen.ts` uit plan 01 — `planRosterChange`, `lesGroepFout`.
    - `.planning/phases/01-lesgroepen/01-PATTERNS.md` sectie
      `providers/SimpleDataProvider.tsx` — de analogen staan er verbatim, inclusief de
      waarschuwing over de vergeten export of afhankelijkheid.
    - `.planning/codebase/ARCHITECTURE.md` — de snapshot-atomiciteit.
  </read_first>
  <action>
    Voeg vier acties toe, elk in de bestaande vorm: lees `storeRef.current`, stop bij `null`,
    reken de volgende volledige snapshot uit, roep `commit()` precies één keer aan.

    `addLesGroep(g: Omit<LesGroep, 'id'>): Promise<LesGroep | null>` — roept `lesGroepFout`
    aan, zet bij een melding `setError` met die melding en geeft `null` terug, precies zoals
    `addBooking` dat doet bij een dubbele boeking. Anders: `newId('lg')`, `created_at` op nu,
    toevoegen aan `store.lesGroepen`, één commit, de aangemaakte groep terug.

    `updateLesGroep(id: string, patch: Partial<Omit<LesGroep, 'id' | 'roster'>>)` — patcht de
    groepsrij en niets anders. `roster` is met opzet uitgesloten van het type, om dezelfde
    reden waarom `updateBooking` `payment_method` uitsluit: het rooster heeft een eigen weg
    omdat er meer aan hangt dan de rij zelf. Zet die reden in het commentaar.

    `updateLesGroepRoster(id: string, nieuwRooster: string[])` — de enige actie met echt
    rekenwerk. Roept `planRosterChange` uit `lib/lesgroepen.ts` aan met de groep, het nieuwe
    rooster, `store.bookings` en `new Date()`, en past het plan toe: de gewijzigde groep in
    `lesGroepen` én de `bookingPatches` in `bookings`, in één `commit()`. Reken de patches niet
    zelf uit in dit bestand — dat is precies wat `lib/lesgroepen.ts` doet en waarom het getest
    is. Zet er het commentaar bij dat de twee verzamelingen samen in één stap gaan omdat een
    half doorgevoerde roosterwijziging een groep zou opleveren die niet klopt met zijn lessen.

    `archiveLesGroep(id: string, gearchiveerd: boolean)` — zet uitsluitend `archived` om.
    Commentaar: archiveren raakt geen enkele boeking en wist het rooster niet; wie later
    terugkijkt hoort nog te zien wie erin zat (GROEP-07).

    Vul in de terugval voor de instellingen op regel 1113 `lesson_duration_minutes: 60` aan, met
    dezelfde redenering als de rest van dat object: een club waarvan `club_settings` deze sleutel
    nog niet kent hoort 60 minuten te krijgen en geen `undefined` (D-05).

    Exporteer alle vier de acties in de context-waarde, zet ze in het type van de context, en
    zet ze in de `useMemo`-afhankelijkheidslijst. Een vergeten regel daar is de meest voorkomende
    manier waarop een nieuwe actie stilzwijgend nooit op een scherm aankomt.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test && npx expo export --platform web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "addLesGroep\|updateLesGroep\|updateLesGroepRoster\|archiveLesGroep" providers/SimpleDataProvider.tsx` geeft minstens 12 (elke actie komt voor in de definitie, in het contexttype, in de context-waarde en in de afhankelijkheidslijst).
    - `grep -c "planRosterChange" providers/SimpleDataProvider.tsx` geeft 1, en de import komt
      uit `../lib/lesgroepen`.
    - `updateLesGroepRoster` bevat precies één aanroep van `commit(`, en die aanroep bevat zowel
      `lesGroepen:` als `bookings:`.
    - Het type van `updateLesGroep` sluit `roster` uit via `Omit<..., 'id' | 'roster'>`.
    - `grep "lesson_duration_minutes: 60" providers/SimpleDataProvider.tsx` vindt een regel op
      de terugval voor de instellingen.
    - `npx tsc --noEmit` schoon, `npm test` groen, `npx expo export --platform web` slaagt.
    - Geen enkele actie berekent zelf welke boekingen geraakt worden; dat komt uit
      `lib/lesgroepen.ts`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Supabase | `saveToSupabase` schrijft met een upsert; de RLS-policies uit plan 02 zijn de bewaking. Deze laag stuurt alleen rijen. |
| lokale opslag → app | `withDefaults` is de grens tussen een oude, op de telefoon bewaarde blob en de huidige code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Denial of Service | Een club met een oudere lokale opslag krijgt `undefined` in plaats van een lege lijst en elk lesgroepscherm valt om op `.map` | mitigate | `lesGroepen: data.lesGroepen ?? []` in `withDefaults`, met het commentaar dat de reden noemt |
| T-01-09 | Denial of Service | Een club die de migratie nog niet draaide laat de hele `loadFromSupabase` stuklopen op een onbekende tabel | mitigate | `selectAllOptioneel<LesGroep>('lesson_groups')`, verplicht per D-11 — dezelfde weg als `ouder_kind` en `coach_rates` |
| T-01-10 | Tampering | Een half doorgevoerde roosterwijziging laat de groep en zijn lessen uit elkaar lopen | mitigate | Eén `commit()` per actie, met `lesGroepen` en `bookings` in dezelfde snapshot — de snapshot-atomiciteit uit ARCHITECTURE.md |
| T-01-11 | Elevation of Privilege | Een niet-beheerder roept `addLesGroep` aan via een scherm dat de grens niet legt | transfer | De databank is de bewaker: `lesson_groups_write` op `is_admin()` (plan 02). De schermgrens komt in plan 04/05; deze laag is bewust geen bewaker, zoals de rest van de provider |
| T-01-SC | Tampering | npm/pip/cargo-installaties | accept | Dit plan installeert niets; deze fase voegt nul pakketten toe |
</threat_model>

<verification>
- `npx tsc --noEmit` schoon.
- `npm test` volledig groen, inclusief de nieuwe `lib/sync`-gevallen.
- `npx expo export --platform web` slaagt.
- `grep -rn "lesGroepen" lib/sync.ts providers/mockStore.ts providers/supabaseStore.ts providers/SimpleDataProvider.tsx` toont alle vier de stops uit de wiringchecklist.
- Er is geen SQL gedraaid en er is niets naar de productiedatabank geschreven.
</verification>

<success_criteria>
- Alle vier de stops uit de wiringchecklist zijn gezet: types (plan 01), sync, beide stores,
  provider.
- `lesson_groups` wordt met `selectAllOptioneel` geladen, zodat een niet-gedraaide migratie
  de app niet breekt.
- `updateLesGroepRoster` laat het rekenwerk aan `lib/lesgroepen.ts` en commit één keer.
- De vier acties zijn bereikbaar vanuit `useSimpleData()`.
</success_criteria>

<output>
Create `.planning/phases/01-lesgroepen/01-03-SUMMARY.md` when done
</output>
