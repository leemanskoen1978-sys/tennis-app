---
phase: 01-lesgroepen
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase-schema.sql
autonomous: true
requirements: [TOEG-03]
must_haves:
  truths:
    - "supabase-schema.sql bevat de tabel lesson_groups als idempotent blok dat de gebruiker zelf kan draaien"
    - "bookings krijgt een group_id-kolom via alter table add column if not exists"
    - "lesson_groups heeft RLS aan met twee policies die alleen naar is_admin() kijken"
    - "Geen enkele policy op lesson_groups verwijst naar de maker van de rij"
  artifacts:
    - path: "supabase-schema.sql"
      provides: "lesson_groups-tabel, bookings.group_id, index en admin-only RLS"
      contains: "create table if not exists lesson_groups"
  key_links:
    - from: "supabase-schema.sql"
      to: "is_admin()"
      via: "using en with check op beide policies"
      pattern: "using \\(is_admin\\(\\)\\) with check \\(is_admin\\(\\)\\)"
    - from: "bookings.group_id"
      to: "lesson_groups(id)"
      via: "foreign key on delete set null"
      pattern: "group_id text references lesson_groups\\(id\\) on delete set null"
---

<objective>
Het schema van de lesgroepen als tekst onderaan `supabase-schema.sql`, klaar om door de
gebruiker zelf gedraaid te worden.

Purpose: TOEG-03 en D-11. De gebruiker draait migraties met de hand in de SQL-editor van
Supabase; de dev-server praat met de échte clubdatabank, dus niets in deze fase mag daar
ongevraagd aan komen. Dit plan levert alleen tekst. Het draaien ervan is plan 07.
Output: één idempotent blok onderaan `supabase-schema.sql` met de tabel, de kolom op
`bookings`, de index en twee RLS-policies die aantoonbaar immuun zijn voor de upsert-val.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-lesgroepen/01-CONTEXT.md
@.planning/phases/01-lesgroepen/01-RESEARCH.md
@.planning/phases/01-lesgroepen/01-PATTERNS.md
@.planning/codebase/CONCERNS.md

<interfaces>
De kolomnamen moeten exact overeenkomen met de `LesGroep`-interface uit plan 01 taak 1,
want `lib/sync.ts` schrijft de rij één op één weg:

  id text primary key
  name text not null
  level text not null
  weekday int (0-6)
  start_hour int (0-23)
  start_minute int (0-59, default 0)
  coach_id text references users(id) on delete set null
  court_id text references courts(id) on delete set null
  season_start date not null
  season_end date not null
  roster jsonb not null default '[]'::jsonb
  archived boolean not null default false
  created_at timestamptz not null default now()

En op bookings: group_id text references lesson_groups(id) on delete set null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Taak 1: lesson_groups, bookings.group_id en de admin-only policies</name>
  <files>supabase-schema.sql</files>
  <read_first>
    - `supabase-schema.sql` regels 1-23 — de drie genummerde ontwerpkeuzes bovenaan. Keuze 1
      (sleutels zijn `text`, de app maakt ze zelf) en keuze 3 (lijstjes die alleen als geheel
      betekenis hebben staan als jsonb) zijn de rechtvaardiging voor `id text` en voor
      `roster jsonb`.
    - `supabase-schema.sql` regels 187-191 — de `coach_rates`-tabel: geen `created_by`, geen
      eigenaarskolom, niets.
    - `supabase-schema.sql` regels 636-646 — `rates_select` en `rates_write`. `rates_write` is
      letterlijk de vorm die beide nieuwe policies moeten krijgen.
    - `supabase-schema.sql` regels 729-749 — `bookings_insert` en het commentaar erboven over
      de upsert-val. Dit is het incident waar het commentaar bij de nieuwe policies naar moet
      terugwijzen.
    - `supabase-schema.sql` regels 391-470 — de trigger `bewaak_betaalvelden`. Lees waarom een
      nieuwe kolom hier vanzelf onder "mag niet" valt voor een speler, en dat de beheerder en
      de trainer van de les er sowieso langs mogen.
    - `supabase-schema.sql` regel 71 — `court_id text references courts(id) on delete set null`,
      de vorm die `group_id` op `bookings` moet spiegelen.
    - `.planning/phases/01-lesgroepen/01-PATTERNS.md` sectie `supabase-schema.sql` — het
      volledige aanbevolen SQL-blok staat er verbatim in.
    - `.planning/phases/01-lesgroepen/01-CONTEXT.md` D-10, D-11.
  </read_first>
  <action>
    Voeg onderaan `supabase-schema.sql` één nieuw blok toe, na alles wat er nu staat, met een
    kopregel-commentaar in de stijl van de rest van het bestand.

    Het blok bevat, in deze volgorde: `create table if not exists lesson_groups` met de kolommen
    uit het `<interfaces>`-blok inclusief de `check`-beperkingen op `weekday`, `start_hour` en
    `start_minute`; `alter table bookings add column if not exists group_id text references
    lesson_groups(id) on delete set null`; `create index if not exists bookings_group_idx on
    bookings (group_id)`; `alter table lesson_groups enable row level security`; en dan
    `lesson_groups_select` en `lesson_groups_write`, elk voorafgegaan door een
    `drop policy if exists`, precies zoals elke andere policy in dit bestand.

    Beide policies kijken alleen naar `is_admin()`: `lesson_groups_select` met `for select to
    authenticated using (is_admin())`, `lesson_groups_write` met `for all to authenticated
    using (is_admin()) with check (is_admin())`. Er staat nergens een `created_by`, een
    `app_user_id()`-vergelijking of enige andere verwijzing naar wie de rij ooit heeft gemaakt.

    Het Nederlandse commentaar boven het blok moet drie dingen zeggen, want dat commentaar is
    de bewaking voor wie hier later aan komt. Eén: wat een lesgroep is en dat hij losstaat van
    de losse les, met verwijzing naar D-01/D-02 in
    `.planning/phases/01-lesgroepen/01-CONTEXT.md`. Twee: waarom `roster` jsonb is en geen
    koppeltabel — ontwerpkeuze 3 bovenaan dit bestand, de lijst wordt nooit los van zijn groep
    opgevraagd. Drie, en dat is het belangrijkste: waarom hier géén eigenaarscontrole staat, met
    een expliciete verwijzing naar het commentaar boven `bookings_insert` — de app schrijft met
    een upsert, Postgres toetst de `with check` ook bij een latere wijziging, en alles wat over
    de máker van de rij wordt geëist geldt dus ook voor iedere volgende beheerder. Dat heeft dit
    project al twee keer stilzwijgend gebroken. Zet er de instructie bij: wie hier een conditie
    aan wil toevoegen, leest eerst dat commentaar.

    Voeg bij het blok ook een korte notitie waarom `bewaak_betaalvelden` níet aangepast hoeft te
    worden: die trigger vergelijkt de hele rij minus drie velden, dus `group_id` valt er vanzelf
    onder "mag een speler niet wijzigen", terwijl de beheerder en de trainer van de les er
    bovenaan de functie al langs mogen. Wie de groep van een les verzet, is per definitie een
    van die twee.

    Draai deze SQL niet. Verbind niet met Supabase. Dit plan levert tekst; de gebruiker draait
    het blok zelf in plan 07.
  </action>
  <verify>
    <automated>grep -c "create table if not exists lesson_groups" supabase-schema.sql; grep -c "created_by" supabase-schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "create table if not exists lesson_groups" supabase-schema.sql` geeft 1.
    - `grep -c "alter table bookings add column if not exists group_id" supabase-schema.sql` geeft 1.
    - `grep -c "create index if not exists bookings_group_idx" supabase-schema.sql` geeft 1.
    - `grep -c "alter table lesson_groups enable row level security" supabase-schema.sql` geeft 1.
    - `sed -n '/create policy lesson_groups_select/,/;/p' supabase-schema.sql` bevat
      `using (is_admin())` en bevat niet `created_by` en niet `app_user_id`.
    - `sed -n '/create policy lesson_groups_write/,/;/p' supabase-schema.sql` bevat
      `using (is_admin()) with check (is_admin())` en bevat niet `created_by` en niet
      `app_user_id`.
    - Elke `create policy` op `lesson_groups` wordt voorafgegaan door een `drop policy if
      exists` met dezelfde naam.
    - De kolomnamen in het `create table`-blok komen één op één overeen met de velden van
      `LesGroep` uit plan 01: `grep -o "name\|level\|weekday\|start_hour\|start_minute\|coach_id\|court_id\|season_start\|season_end\|roster\|archived"` binnen het blok levert alle elf namen.
    - Het commentaar boven het blok bevat het woord `bookings_insert` (de kruisverwijzing naar
      het upsert-incident) en verwijst naar `01-CONTEXT.md`.
    - Er is geen `unique`-beperking op de combinatie naam/weekdag/beginuur toegevoegd.
    - Er is geen verbinding met Supabase gemaakt en er is geen SQL uitgevoerd.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/app → Postgres | Elke lees- en schrijfactie van een ingelogde gebruiker op `lesson_groups` passeert RLS. Dit is de echte bewaking; de app is dat niet (`lib/rechten.ts`-kop). |
| SQL-editor → Postgres | De gebruiker draait dit blok zelf met verhoogde rechten. Buiten een sessie geldt `bewaak_betaalvelden` niet (`auth.uid() is null`) — bewust. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation of Privilege | Een gewone trainer of speler leest of schrijft `lesson_groups` langs de app om, rechtstreeks via de Supabase-API | mitigate | `lesson_groups_select` en `lesson_groups_write` staan beide op `is_admin()`; RLS staat aan op de tabel. Handmatig nagelopen in plan 07, stap 4 |
| T-01-05 | Tampering | De upsert-val: een `with check` met een eigenaarsconditie laat een latere wijziging door een andere beheerder stil mislukken | mitigate | Beide policies bevatten geen enkele verwijzing naar de maker van de rij — de vorm van `rates_write`. Handmatig bewezen in plan 07, stap 2 en 5 |
| T-01-06 | Tampering | Een speler wijzigt via `bookings_update` de nieuwe kolom `group_id` en hangt zijn les aan een andere groep | mitigate | `bewaak_betaalvelden` vergelijkt de hele rij minus `payment_method`, `beurtenkaart_id` en `attendance`; `group_id` valt daar vanzelf buiten en wordt geweigerd. Geen triggerwijziging nodig — dat is precies waarom die trigger zo geschreven is |
| T-01-07 | Denial of Service | Een `select` op `lesson_groups` tegen een club die de migratie nog niet draaide laat de hele lading stuklopen | mitigate | Niet hier, maar in plan 03: `selectAllOptioneel` in plaats van `selectAll`, verplicht per D-11 |
| T-01-SC | Tampering | npm/pip/cargo-installaties | accept | Dit plan installeert niets; deze fase voegt nul pakketten toe (RESEARCH.md, Package Legitimacy Audit) |
</threat_model>

<verification>
- `git diff supabase-schema.sql` toont uitsluitend toevoegingen onderaan het bestand; geen
  enkele bestaande regel, policy of trigger is gewijzigd.
- `npx tsc --noEmit` en `npm test` blijven groen (dit plan raakt geen TypeScript).
- Er staat geen SQL-uitvoering in de uitvoeringslog.
</verification>

<success_criteria>
- Het blok is idempotent: alles staat als `if not exists` of met een `drop policy if exists`
  ervoor, en het bestand mag opnieuw gedraaid worden zoals de kop van het bestand belooft.
- Geen enkele policy op `lesson_groups` kent het begrip "maker van de rij".
- De kolomnamen sluiten aan op `LesGroep` uit plan 01.
- De migratie is geschreven maar niet gedraaid.
</success_criteria>

<output>
Create `.planning/phases/01-lesgroepen/01-02-SUMMARY.md` when done
</output>
