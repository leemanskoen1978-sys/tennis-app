---
phase: 04-excel-export
plan: 05
type: execute
wave: 5
depends_on: [04-04]
files_modified:
  - app/admin/export.tsx
  - app/admin/index.tsx
  - lib/i18n-en.ts
autonomous: false
requirements: [EXP-01]

must_haves:
  truths:
    - "De beheerder kiest in Beheer een periode en downloadt daar één Excel-bestand van met vier bladen."
    - "Een trainer die de link intikt krijgt het scherm niet te zien, ook niet als de tegel bij hem verborgen is."
    - "Op een telefoon staat er geen knop die het toch niet kan, maar een zin die zegt waarom."
    - "Het bestand heet naar zijn periode, zodat twee exports in een downloadmap uit elkaar te houden zijn."
  artifacts:
    - path: "app/admin/export.tsx"
      provides: "Het exportscherm met periodekeuze, beheerdersgrens en downloadknop"
      contains: "isAdmin"
    - path: "app/admin/index.tsx"
      provides: "De tegel naar het exportscherm, alleen voor de beheerder"
      contains: "admin/export"
    - path: "lib/i18n-en.ts"
      provides: "De Engelse kant van elke nieuwe zin op dit scherm"
  key_links:
    - from: "app/admin/export.tsx"
      to: "lib/export-trainingen.ts::exportWerkmap"
      via: "de vier bladen samenstellen"
      pattern: "exportWerkmap\\("
    - from: "app/admin/export.tsx"
      to: "lib/share.ts::shareXlsx"
      via: "het bestand bij de gebruiker krijgen"
      pattern: "shareXlsx\\("
    - from: "app/admin/export.tsx"
      to: "lib/period.ts::bookingsInPeriod"
      via: "dezelfde periodefilter als Historiek en Rapport"
      pattern: "bookingsInPeriod\\("
---

<objective>
Het scherm eromheen. `lib/export-trainingen.ts` levert de bytes; hier kiest de beheerder een
periode en krijgt hij het bestand. De grens staat op het scherm zelf en niet alleen op de
tegel (D-06): deze fase voegt geen tabel toe en dus ook geen RLS-policy die de fout achteraf
nog opvangt.

Purpose: EXP-01 — één periode, één bestand.
Output: `app/admin/export.tsx`, een tegel in Beheer, de Engelse zinnen, en een menselijke
controle dat het bestand in Excel opent zoals bedoeld.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-excel-export/04-CONTEXT.md
@.planning/phases/04-excel-export/04-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/04-excel-export/04-04-SUMMARY.md

<interfaces>
// lib/export-trainingen.ts — uit plan 04-04
export interface ExportGegevens { bookings: Booking[]; users: User[]; courts: Court[]; groepen: LesGroep[] }
export function exportWerkmap(g: ExportGegevens): Uint8Array;

// lib/share.ts — DE weg naar de gebruiker (D-13). lib/bestand.ts LEEST alleen; niet gebruiken.
export const xlsxWordtOndersteund: boolean;
export async function shareXlsx(filename: string, bytes: Uint8Array): Promise<void>;

// lib/period.ts
export interface Period { kind: PeriodKind; from: Date; to: Date }
export function currentPeriod(now?: Date): Period;
export function bookingsInPeriod(bookings: Booking[], p: Period): Booking[];
export function periodLabel(p: Period): string;
export function periodFilename(p: Period, extensie?: 'csv' | 'xlsx'): string;

// components/ui/PeriodPicker.tsx
export function PeriodPicker({ value, onChange }: { value: Period; onChange: (p: Period) => void }): React.JSX.Element;

// lib/rechten.ts
export function isAdmin(user: User | null | undefined): boolean;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Taak 1: het exportscherm app/admin/export.tsx</name>
  <files>app/admin/export.tsx</files>
  <read_first>
    - `app/agenda/historiek.tsx` in zijn geheel — de dichtste analoog: `PeriodPicker`,
      de eigen `exportError`-state naast de globale `error` van de provider, de
      `exporteer`-wikkel met try/catch, en de `xlsxWordtOndersteund`-vertakking rond de knop.
    - `app/admin/lesgroepen/index.tsx` regel 55-70 — de beheerdersgrens woordelijk: een
      vroege `return` met `<Screen scroll={false}>` vóór alle andere schermlogica, met het
      commentaar dat uitlegt dat een verborgen tegel geen toegangscontrole is.
    - `app/admin/reports.tsx` — de vorm van een beheerdersrapportscherm (`Screen`, `Card`,
      `PeriodPicker`, `useSimpleData`).
    - `lib/share.ts` in zijn geheel — vooral het commentaar bij `xlsxWordtOndersteund` over
      waarom een telefoon geen xlsx aankan.
    - `.planning/phases/04-excel-export/04-CONTEXT.md`, D-06, D-12 en D-13.
    - `.planning/phases/04-excel-export/04-PATTERNS.md`, sectie "`app/admin/export.tsx` (new)".
    - `providers/SimpleDataProvider.tsx` — de namen van de velden die `useSimpleData()`
      teruggeeft (`currentUser`, `users`, `courts`, `bookings`, `lesGroepen`, `error`);
      neem ze over zoals ze daar heten, verzin er geen.
  </read_first>
  <action>
    Maak `app/admin/export.tsx` met een Nederlands kopcommentaar van 3-5 regels: dit is één
    periode en één bestand met vier bladen, en het is club-breed — niet per trainer, zoals
    Historiek — omdat de import van fase 5 het hele seizoen terugleest.

    De beheerdersgrens staat vóór alle andere schermlogica, in de vorm van
    `app/admin/lesgroepen/index.tsx`: `if (!isAdmin(currentUser)) return <Screen scroll={false}>
    ...</Screen>` met de zin `t('Exporteren is alleen voor de beheerder.')`. Neem het
    commentaar over dat uitlegt waarom de grens hier staat en niet alleen op de tegel — met
    de aanvulling dat deze fase geen eigen tabel heeft en er dus geen policy achter staat die
    de fout alsnog opvangt (D-06).

    Verder: `const [period, setPeriod] = useState<Period>(() => currentPeriod())`, een
    `<PeriodPicker value={period} onChange={setPeriod} />`, en
    `const inPeriode = useMemo(() => bookingsInPeriod(bookings, period), [bookings, period])`.
    Gebruik `useSimpleData()` rechtstreeks en géén `useAgendaScope`/`CoachFilter`: de export
    gaat over alle trainers (D-01).

    Toon in een `Card` de omvang van wat er gedownload gaat worden — het aantal lessen en het
    aantal groepen in de periode — zodat de beheerder ziet dat hij de goede periode heeft
    voor hij klikt. Reken dat niet in dit scherm uit voorbij een `.length`; alles wat verder
    gaat hoort in `lib/`.

    De knop, in de vorm van `historiek.tsx`: alleen tonen als `xlsxWordtOndersteund`,
    `disabled` zolang er geen lessen in de periode zijn, en bij indrukken
    `shareXlsx(periodFilename(period, 'xlsx'), exportWerkmap({ bookings: inPeriode, users,
    courts, groepen: lesGroepen }))` in een lokale `exporteer`-wikkel met try/catch die de
    fout in een eigen `exportError`-state zet. Importeer `lib/bestand.ts` niet — dat leest
    alleen bestanden (D-13).

    Is `xlsxWordtOndersteund` onwaar (een telefoon), toon dan geen knop maar de zin
    `t('Een Excel-bestand maken kan alleen op de website.')`.

    Zet onder de knop een korte zin die zegt wat er in het bestand zit: de vier bladen bij
    naam en de gekozen periode via `periodLabel(period)`. Vermeld daarbij dat blad
    "Aanwezigheid" ook leeg uit te printen is voor een vervanger (D-12) — dat is de reden dat
    het blad bestaat en niemand raadt dat vanzelf.

    Alle nieuwe schermteksten door `t('Nederlandse zin')`. Gebruik `Screen`, `Card` en
    `Button` uit `components/ui/`; verzin geen nieuwe kleuren of maten, neem de tokens uit
    `constants/theme.ts` en `constants/tennis-colors.ts`.
  </action>
  <verify>
    <automated>cd "/Users/leko/Downloads/tennis app" && npx tsc --noEmit && npx expo export --platform web --output-dir .webbuild-check && rm -rf .webbuild-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` geeft nul fouten en `npx expo export --platform web` slaagt.
    - `grep -c 'isAdmin' app/admin/export.tsx` is minstens `1`, en de vroege `return` staat
      vóór de eerste `useMemo`/`bookingsInPeriod`-aanroep in het bestand:
      `grep -n 'isAdmin\|bookingsInPeriod' app/admin/export.tsx | head -1` noemt `isAdmin`.
    - `grep -c "lib/bestand" app/admin/export.tsx` geeft `0` (D-13).
    - `grep -c 'shareXlsx\|xlsxWordtOndersteund' app/admin/export.tsx` is minstens `2`.
    - `grep -c 'exportWerkmap' app/admin/export.tsx` is minstens `1`.
    - `grep -c 'useAgendaScope\|CoachFilter' app/admin/export.tsx` geeft `0` — de export is club-breed.
    - `grep -cE '"[A-Z][a-z]+ [a-z]+"' app/admin/export.tsx` levert geen harde Engelse
      schermtekst op; elke gebruikerszin staat in `t('...')`.
    - `grep -c 'buildXlsx\|buildWorkbook\|payoutsByCoach\|lesgeverId' app/admin/export.tsx`
      geeft `0` — het scherm rekent niets uit en stelt geen blad samen.
  </acceptance_criteria>
  <done>
    Het scherm bestaat, staat achter `isAdmin` vóór alle andere logica, kiest een periode met
    de bestaande `PeriodPicker`, roept `exportWerkmap` aan en levert het bestand via
    `shareXlsx` — en op een telefoon staat er een zin in plaats van een knop die faalt.
  </done>
</task>

<task type="auto">
  <name>Taak 2: de tegel in Beheer en de Engelse zinnen</name>
  <files>app/admin/index.tsx, lib/i18n-en.ts</files>
  <read_first>
    - `app/admin/index.tsx` regel 55-105 — de drie tegelgroepen (`geld`, `club`, `systeem`)
      en het `...(isAdmin(currentUser) ? [{...} as Tile] : [])`-blok dat `lesgroepen` en
      `leden` gebruiken; dat is de analoog, letterlijk over te nemen.
    - `lib/i18n-en.ts` regel 1-20 — het kopcommentaar: de sleutel is de Nederlandse zin
      letterlijk, inclusief hoofdletters en leestekens, en een ontbrekende zin is een
      zichtbaar gat en geen stille fout.
    - `app/admin/export.tsx` zoals taak 1 hem oplevert — de lijst zinnen die vertaald moeten.
  </read_first>
  <action>
    Voeg in `app/admin/index.tsx` een tegel toe in de groep `club`, in hetzelfde
    `...(isAdmin(currentUser) ? [...] : [])`-blok als `lesgroepen` en `leden`, die naar
    `/admin/export` navigeert. Titel `t('Trainingen exporteren')`, ondertitel
    `t('Eén Excel-bestand per periode')`. Kies een icoon uit `lucide-react-native` dat nog
    niet in dit bestand gebruikt wordt en voeg het aan de bestaande importregel toe.

    Zet er een kort Nederlands commentaar bij, in de toon van de buren: dat de tegel hier
    wegblijft voor een gewone trainer is wellevendheid en geen bewaking — het scherm zelf
    doet dat werk (D-06). Verzin geen tweede grens en verplaats de bestaande niet.

    Voeg in `lib/i18n-en.ts` een Engelse tegenhanger toe voor élke nieuwe Nederlandse zin uit
    taak 1 en uit deze taak, met de Nederlandse zin letterlijk als sleutel. Zet ze bij een
    bestaande, passende sectie in dat bestand; maak geen nieuwe sectie als er een past.
  </action>
  <verify>
    <automated>cd "/Users/leko/Downloads/tennis app" && npx tsc --noEmit && npm test && npx expo export --platform web --output-dir .webbuild-check && rm -rf .webbuild-check</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` geeft nul fouten, `npm test` is groen, `npx expo export --platform web` slaagt.
    - `grep -c "admin/export" app/admin/index.tsx` is minstens `1`.
    - De tegel staat in een beheerdersblok: `grep -n "admin/export" app/admin/index.tsx` wijst
      naar een regel binnen een `isAdmin(currentUser)`-vertakking.
    - Elke Nederlandse zin uit `app/admin/export.tsx` heeft een sleutel in `lib/i18n-en.ts`:
      `for s in $(grep -o "t('[^']*'" app/admin/export.tsx | sed "s/t('//;s/'$//"); do grep -q "$s" lib/i18n-en.ts || echo "ONVERTAALD: $s"; done` geeft geen uitvoer.
    - `grep -c "Trainingen exporteren" lib/i18n-en.ts` is minstens `1`.
  </acceptance_criteria>
  <done>
    De beheerder vindt de export als tegel in Beheer → Club, en elke nieuwe zin heeft zijn
    Engelse tegenhanger.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Taak 3: met de hand nalopen — het bestand in Excel en de grens op het scherm</name>
  <files>—</files>
  <read_first>
    - `.planning/phases/04-excel-export/04-RESEARCH.md`, sectie "Validation Architecture" →
      "Manual-only steps": wat `tsc` en Jest per definitie niet kunnen zien.
    - `.planning/phases/04-excel-export/04-CONTEXT.md`, D-01, D-03, D-06, D-12.
    - `.planning/IMPORT-SJABLOON.md`, "De kolommen".
  </read_first>
  <what-built>
    Een exportscherm in Beheer → Club → "Trainingen exporteren" dat over een gekozen periode
    één xlsx maakt met de bladen Lessen, Uren per trainer, Aanwezigheid en Groepen, achter
    de beheerdersgrens, geleverd via de browserdownload.
  </what-built>
  <action>
    Pauzeer en laat de beheerder de negen stappen hieronder zelf doorlopen. Er wordt in deze
    taak niets geschreven: dit zijn precies de dingen die `npx tsc --noEmit` en Jest per
    definitie niet kunnen zien — of Excel de datums werkelijk als datums leest, of de vier
    tabbladen er staan zoals bedoeld, en of een trainer die de link intikt buiten blijft.
    Verwerk bevindingen pas na het antwoord van de gebruiker.
  </action>
  <how-to-verify>
    1. Start `npx expo start --web`. Let op: de dev-server praat met de échte Supabase van de
       club — kijk en download alleen, wijzig niets.
    2. Ga naar Beheer → Club → "Trainingen exporteren". Kies "Vorige maand" en klik de
       Excel-knop. Verwacht: een bestand `lessen-JJJJ-MM.xlsx` in je downloads.
    3. Open het bestand. Verwacht onderin vier tabbladen, in deze volgorde:
       `Lessen`, `Uren per trainer`, `Aanwezigheid`, `Groepen`.
    4. Blad "Lessen": klik een cel in de kolom `Datum` aan. Verwacht een datum, geen getal en
       geen tekst — sorteren op die kolom moet chronologisch gaan. Controleer dat de
       koprij letterlijk deze koppen bevat: `Datum`, `Uur`, `Groep`, `Coach`, `Leerling`,
       `Groep-ID`, `Type les`, `E-mail leerling`, `Baan`. Controleer dat `Locatie` er NIET
       staat. Zoek een groepsles op: die hoort er als meerdere regels te staan met dezelfde
       datum en hetzelfde uur en verschillende leerlingen.
    5. Blad "Uren per trainer": selecteer de kolom `Loon (EUR)`. Verwacht dat Excel onderin
       een som toont. Vergelijk dat totaal met Beheer → Rapport over dezelfde periode: het
       trainersloon hoort gelijk te zijn. Controleer dat er nergens een omzetbedrag staat.
    6. Blad "Aanwezigheid": verwacht per groep een datumrij en daaronder de spelers, met
       `X`/`afw` en lege cellen. Print het blad (of print-voorbeeld): het moet als lege
       invullijst bruikbaar zijn voor een vervanger.
    7. Blad "Groepen": elke rij heeft een gevuld `Groep-ID`.
    8. De grens: log uit en log in als een gewone trainer (niet-beheerder). Tik de link
       `/admin/export` rechtstreeks in de adresbalk. Verwacht: geen exportscherm, maar de zin
       "Exporteren is alleen voor de beheerder."
    9. Open de app op een telefoon of in een smalle simulator. Verwacht: geen Excel-knop die
       faalt, maar de zin dat dit alleen op de website kan.
  </how-to-verify>
  <acceptance_criteria>
    - Vier tabbladen, met exact die namen en in die volgorde.
    - `Datum` sorteert chronologisch; `Loon (EUR)` telt op met de somfunctie van Excel.
    - Het loontotaal komt overeen met Beheer → Rapport over dezelfde periode.
    - Blad "Lessen" bevat de negen genoemde koppen en géén `Locatie`.
    - Een groepsles staat als meerdere regels met dezelfde datum en hetzelfde uur.
    - Elke rij op blad "Groepen" heeft een `Groep-ID`.
    - Een niet-beheerder krijgt op `/admin/export` alleen de weigerzin te zien.
    - Op een telefoon staat er een uitleg in plaats van een knop.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — dit is met opzet een menselijke controle: Excel openen, kolommen
    optellen en een niet-beheerder de link laten intikken zijn niet te automatiseren binnen
    deze codebase. De automatiseerbare helft staat in taak 1 en taak 2 en in
    `<verification>` onderaan dit plan.</automated>
    <human-check>De negen stappen onder `<how-to-verify>`, met de uitkomsten onder
    `<acceptance_criteria>`.</human-check>
  </verify>
  <done>
    De beheerder heeft de negen stappen doorlopen en bevestigt elk punt onder
    `<acceptance_criteria>`, of heeft per punt beschreven wat er anders is.
  </done>
  <resume-signal>Typ "goedgekeurd", of beschrijf per punt wat er anders is dan hierboven staat.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gebruiker → exportscherm | Een ingelogde trainer kan `/admin/export` intikken; de enige grens is het scherm zelf, want deze fase voegt geen tabel en dus geen RLS-policy toe (D-06). |
| app → browser (download) | De bytes verlaten de app als bestand op de schijf van de beheerder. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Elevation of Privilege | `app/admin/export.tsx` | mitigate | `if (!isAdmin(currentUser)) return <Screen …>` als eerste, vóór elke andere schermlogica — precies het patroon van `app/admin/lesgroepen/index.tsx`; taak 3 loopt het met de hand na via de rechtstreekse URL |
| T-04-13 | Information Disclosure | verborgen tegel als enige grens | mitigate | De tegel verbergen is wellevendheid; het scherm doet de bewaking. Beide staan er, en het commentaar zegt welke van de twee telt |
| T-04-14 | Information Disclosure | club-breed bestand op de schijf | accept | De beheerder exporteert de gegevens van zijn eigen club, die hij in Beheer al ziet; het bestand is geen andere grens dan het scherm |
| T-04-15 | Denial of Service | een export van een heel seizoen op een telefoon | mitigate | De knop bestaat daar niet (`xlsxWordtOndersteund`), met een zin die zegt waarom |
| T-04-SC | Tampering | npm-installs | mitigate | Dit plan installeert niets; `git diff package.json` moet leeg zijn (D-04) |
</threat_model>

<verification>
```bash
cd "/Users/leko/Downloads/tennis app"
npx tsc --noEmit
npm test
npx expo export --platform web --output-dir .webbuild-check && rm -rf .webbuild-check
git diff --stat package.json package-lock.json          # moet leeg zijn
git diff --stat supabase-schema.sql                     # moet leeg zijn: geen tabel, geen policy
```
</verification>

<success_criteria>
- De beheerder kiest een periode en krijgt daar één Excel-bestand van met vier bladen (EXP-01).
- Een niet-beheerder krijgt het scherm niet te zien, ook niet via de rechtstreekse link (D-06).
- Op een telefoon staat er een uitleg in plaats van een knop die faalt.
- Het bestand heet naar zijn periode (`periodFilename`).
- Geen nieuwe afhankelijkheid, geen SQL, geen wijziging aan `supabase-schema.sql`.
- `npx tsc --noEmit`, `npm test` en `npx expo export --platform web` slagen.
</success_criteria>

<output>
Create `.planning/phases/04-excel-export/04-05-SUMMARY.md` when done
</output>
